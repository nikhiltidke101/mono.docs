---
title: schedules
description: Management APIs for availability schedules.
toc: false
---

import { Root, API, APIInfo, APIExample, Property } from 'fumadocs-ui/components/api'
import { Tabs, Tab } from 'fumadocs-ui/components/tabs'
import { Accordion, Accordions } from 'fumadocs-ui/components/accordion';

<Root>

<API>

<APIInfo method={"POST"} route={"/org-schedule-fragments.create"}>

## org-schedule-fragments-create

Creates an organization schedule fragment.

### Request Body (Optional)



<Property required={true} deprecated={false} name={"from"} type={"string"}>

Date (inclusive) on which the organization schedule fragment
begins.


<span>Example:  `"2023-01-01T12:00:00.000Z"`</span>

<span>Format:  `"date-time"`</span>

</Property>

<Property required={true} deprecated={false} name={"intervals"} type={"array of object"}>

The intervals that comprise the schedule fragment.

<Accordions>

<Accordion title={"Object Type"}>

<Property required={true} deprecated={false} name={"from"} type={"string"}>

Date (inclusive) on which the interval begins.

<span>Example:  `"2023-01-01T12:00:00.000Z"`</span>

<span>Format:  `"date-time"`</span>

</Property>

<Property required={false} deprecated={false} name={"is_excluded"} type={"boolean"}>

If true, no organization schedule is looked up for these days and
they are marked as holidays.


</Property>

<Property required={true} deprecated={false} name={"name"} type={"string"}>

The name of the period, for example the event or holiday it
represents.


</Property>

<Property required={false} deprecated={false} name={"to"} type={"string"}>

Date (exclusive) on which the interval ends. If omitted, it is a
single day interval.


<span>Example:  `"2023-01-01T12:00:00.000Z"`</span>

<span>Format:  `"date-time"`</span>

</Property>

</Accordion>

</Accordions>

</Property>

<Property required={true} deprecated={false} name={"name"} type={"string"}>

Name of the organization schedule fragment.

</Property>

<Property required={false} deprecated={false} name={"region_codes"} type={"array of string"}>

CLDR region code of the countries/regions it is meant to be valid
for. Does not drive logic, serves only for easier filtering and
organization.


</Property>

<Property required={true} deprecated={false} name={"to"} type={"string"}>

Date (exclusive) on which the organization schedule fragment's
validity ends.


<span>Example:  `"2023-01-01T12:00:00.000Z"`</span>

<span>Format:  `"date-time"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `201` | Success. |
| `400` | 400 Bad Request. |
| `401` | 401 Unauthorized. |
| `403` | 403 Forbidden. |
| `429` | 429 Too Many Requests. |
| `500` | 500 Internal Server Error. |
| `503` | 503 Service Unavailable. |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X POST "https://api.devrev.ai/org-schedule-fragments.create" \
  -d '{
  "from": "2023-01-01T12:00:00.000Z",
  "intervals": [
    {
      "from": "2023-01-01T12:00:00.000Z",
      "is_excluded": true,
      "name": "string",
      "to": "2023-01-01T12:00:00.000Z"
    }
  ],
  "name": "string",
  "region_codes": [
    "string"
  ],
  "to": "2023-01-01T12:00:00.000Z"
}'
```

<Tabs items={["201","400","401","403","429","500","503"]}>

<Tab value={"201"}>



```json title="Example Response"
{
  "org_schedule_fragment": {
    "created_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "created_date": "2023-01-01T12:00:00.000Z",
    "display_id": "string",
    "id": "string",
    "modified_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "modified_date": "2023-01-01T12:00:00.000Z",
    "from": "2023-01-01T12:00:00.000Z",
    "intervals": [
      {}
    ],
    "name": "string",
    "region_codes": [
      "string"
    ],
    "status": "archived",
    "to": "2023-01-01T12:00:00.000Z"
  }
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type OrgScheduleFragment = AtomBase & {
  /**
   * The date (inclusive) on which the organization schedule fragment
   * begins.
   *
   */
  from?: string;
  /**
   * Periods during which the schedule is considered to be 'off' or to
   * be in some specific named period.
   *
   */
  intervals?: OrgScheduleInterval[];
  /**
   * Human-readable name, indicating the purpose of the schedule.
   *
   */
  name?: string;
  /**
   * CLDR region code of the countries/regions it is meant to be valid
   * for. Does not drive logic, serves only for easier filtering and
   * organization.
   *
   */
  region_codes?: string[];
  /**
   * Status determines how an item can be used. In 'draft' status an item
   * can be edited but can't be used. When 'published' the item can longer
   * be edited but can be used. 'Archived' is read-only.
   *
   */
  status: "archived" | "draft" | "published";
  /**
   * The date (exclusive) on which the organization schedule fragment's
   * validity ends.
   *
   */
  to?: string;
};
export type UserSummary = DevUserSummary | RevUserSummary | ServiceAccountSummary | SysUserSummary;
export type DevUserSummary = UserBaseSummary;
export type UserBaseSummary = AtomBaseSummary & {
  /**
   * The user's display name. The name is non-unique and mutable.
   *
   */
  display_name?: string;
  display_picture?: ArtifactSummary;
  /**
   * Email address of the user.
   */
  email?: string;
  /**
   * Full name of the user.
   */
  full_name?: string;
  /**
   * State of the user.
   */
  state?: "active" | "deactivated" | "deleted" | "locked" | "shadow" | "unassigned";
};
export type ArtifactSummary = AtomBaseSummary;
export type RevUserSummary = UserBaseSummary & {
  /**
   * External ref is a mutable unique identifier for a user within the
   * Rev organization from your primary customer record. If none is
   * available, a good alternative is the email address/phone number
   * which could uniquely identify the user. If none is specified, a
   * system-generated identifier will be assigned to the user.
   *
   */
  external_ref?: string;
  rev_org?: OrgSummary;
};
export type OrgSummary = AccountSummary | RevOrgSummary;
export type AccountSummary = OrgBaseSummary;
export type OrgBaseSummary = AtomBaseSummary & {
  /**
   * Name of the Organization.
   */
  display_name?: string;
};
export type RevOrgSummary = OrgBaseSummary;
export type ServiceAccountSummary = UserBaseSummary;
export type SysUserSummary = UserBaseSummary;

export interface OrgScheduleFragmentsCreateResponse {
  org_schedule_fragment: OrgScheduleFragment;
}
export interface AtomBase {
  created_by?: UserSummary;
  /**
   * Timestamp when the object was created.
   */
  created_date?: string;
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
  modified_by?: UserSummary;
  /**
   * Timestamp when the object was last modified.
   */
  modified_date?: string;
}
export interface AtomBaseSummary {
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
}
/**
 * An optionally named period on day granularity.
 */
export interface OrgScheduleInterval {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"400"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "artifact_already_attached_to_a_parent",
  "existing_parent": "string",
  "is_same": true
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorBadRequest = ErrorBase &
  (
    | ErrorBadRequestArtifactAlreadyAttachedToAParent
    | ErrorBadRequestBadRequest
    | ErrorBadRequestInvalidApiVersion
    | ErrorBadRequestInvalidEnumValue
    | ErrorBadRequestInvalidField
    | ErrorBadRequestMissingDependency
    | ErrorBadRequestMissingRequiredField
    | ErrorBadRequestParseError
    | ErrorBadRequestStaleSchemaFragments
    | ErrorBadRequestUnexpectedJsonType
    | ErrorBadRequestValueNotPermitted
  );

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorBadRequestArtifactAlreadyAttachedToAParent {
  /**
   * The existing parent attached to the artifact.
   */
  existing_parent: string;
  /**
   * Whether the existing parent is the same as the new parent.
   *
   */
  is_same: boolean;
}
export interface ErrorBadRequestBadRequest {}
export interface ErrorBadRequestInvalidApiVersion {
  /**
   * The provided API version.
   */
  value: string;
}
export interface ErrorBadRequestInvalidEnumValue {
  /**
   * The allowed values for the field.
   */
  allowed_values: string[];
  /**
   * The field whose enum value is invalid.
   */
  field_name: string;
  /**
   * The value that was received.
   */
  value: string;
}
export interface ErrorBadRequestInvalidField {
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestMissingDependency {
  /**
   * The dependent fields.
   */
  dependencies?: ErrorBadRequestMissingDependencyDependency[];
  /**
   * The field on which the value depends.
   */
  dependent_field_name?: string;
  /**
   * The value which needs to be set of the dependent field.
   */
  dependent_field_value?: string;
  /**
   * The field whose value was received.
   */
  provided_field_name?: string;
  /**
   * The value that was received.
   */
  provided_field_value?: string;
}
export interface ErrorBadRequestMissingDependencyDependency {
  /**
   * The dependent field name.
   */
  field_name: string;
  /**
   * The dependent field value.
   */
  field_value: string;
}
export interface ErrorBadRequestMissingRequiredField {
  /**
   * The missing field's name.
   */
  field_name: string;
}
export interface ErrorBadRequestParseError {}
/**
 * Error indicating that the request contained one or more stale schema
 * fragments, which are schema fragments that have been replaced by a
 * newer version and are therefore considered deprecated schema fragments.
 * The caller should refresh and use the latest schema fragments in their
 * request.
 *
 */
export interface ErrorBadRequestStaleSchemaFragments {}
export interface ErrorBadRequestUnexpectedJsonType {
  actual: "array" | "bool" | "null" | "number" | "object" | "string";
  expected: "array" | "bool" | "null" | "number" | "object" | "string";
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestValueNotPermitted {
  /**
   * The allowed values for the field.
   */
  allowed_values?: string[];
  /**
   * The field whose value is not permitted.
   */
  field_name: string;
  /**
   * The reason the value isn't permitted.
   */
  reason?: string;
}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"401"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "unauthenticated"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorUnauthorized = ErrorBase & ErrorUnauthorizedUnauthenticated;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorUnauthorizedUnauthenticated {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"403"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "forbidden"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorForbidden = ErrorBase & ErrorForbiddenForbidden;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorForbiddenForbidden {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"429"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "retry_after": 0,
  "type": "too_many_requests"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorTooManyRequests = ErrorBase & ErrorTooManyRequestsTooManyRequests;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorTooManyRequestsTooManyRequests {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"500"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "reference_id": "string",
  "type": "internal_error"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorInternalServerError = ErrorBase & ErrorInternalServerErrorInternalError;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorInternalServerErrorInternalError {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"503"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "service_unavailable"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorServiceUnavailable = ErrorBase & ErrorServiceUnavailableServiceUnavailable;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorServiceUnavailableServiceUnavailable {}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"GET"} route={"/org-schedule-fragments.get"}>

## org-schedule-fragments-get

Gets an organization schedule fragment.

### Query Parameters

<Property required={true} deprecated={false} name={"id"} type={"string"}>

Organization schedule Fragment ID.

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Success. |
| `400` | 400 Bad Request. |
| `401` | 401 Unauthorized. |
| `403` | 403 Forbidden. |
| `429` | 429 Too Many Requests. |
| `500` | 500 Internal Server Error. |
| `503` | 503 Service Unavailable. |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X GET "https://api.devrev.ai/org-schedule-fragments.get"
```

<Tabs items={["200","400","401","403","429","500","503"]}>

<Tab value={"200"}>



```json title="Example Response"
{
  "org_schedule_fragment": {
    "created_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "created_date": "2023-01-01T12:00:00.000Z",
    "display_id": "string",
    "id": "string",
    "modified_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "modified_date": "2023-01-01T12:00:00.000Z",
    "from": "2023-01-01T12:00:00.000Z",
    "intervals": [
      {}
    ],
    "name": "string",
    "region_codes": [
      "string"
    ],
    "status": "archived",
    "to": "2023-01-01T12:00:00.000Z"
  }
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type OrgScheduleFragment = AtomBase & {
  /**
   * The date (inclusive) on which the organization schedule fragment
   * begins.
   *
   */
  from?: string;
  /**
   * Periods during which the schedule is considered to be 'off' or to
   * be in some specific named period.
   *
   */
  intervals?: OrgScheduleInterval[];
  /**
   * Human-readable name, indicating the purpose of the schedule.
   *
   */
  name?: string;
  /**
   * CLDR region code of the countries/regions it is meant to be valid
   * for. Does not drive logic, serves only for easier filtering and
   * organization.
   *
   */
  region_codes?: string[];
  /**
   * Status determines how an item can be used. In 'draft' status an item
   * can be edited but can't be used. When 'published' the item can longer
   * be edited but can be used. 'Archived' is read-only.
   *
   */
  status: "archived" | "draft" | "published";
  /**
   * The date (exclusive) on which the organization schedule fragment's
   * validity ends.
   *
   */
  to?: string;
};
export type UserSummary = DevUserSummary | RevUserSummary | ServiceAccountSummary | SysUserSummary;
export type DevUserSummary = UserBaseSummary;
export type UserBaseSummary = AtomBaseSummary & {
  /**
   * The user's display name. The name is non-unique and mutable.
   *
   */
  display_name?: string;
  display_picture?: ArtifactSummary;
  /**
   * Email address of the user.
   */
  email?: string;
  /**
   * Full name of the user.
   */
  full_name?: string;
  /**
   * State of the user.
   */
  state?: "active" | "deactivated" | "deleted" | "locked" | "shadow" | "unassigned";
};
export type ArtifactSummary = AtomBaseSummary;
export type RevUserSummary = UserBaseSummary & {
  /**
   * External ref is a mutable unique identifier for a user within the
   * Rev organization from your primary customer record. If none is
   * available, a good alternative is the email address/phone number
   * which could uniquely identify the user. If none is specified, a
   * system-generated identifier will be assigned to the user.
   *
   */
  external_ref?: string;
  rev_org?: OrgSummary;
};
export type OrgSummary = AccountSummary | RevOrgSummary;
export type AccountSummary = OrgBaseSummary;
export type OrgBaseSummary = AtomBaseSummary & {
  /**
   * Name of the Organization.
   */
  display_name?: string;
};
export type RevOrgSummary = OrgBaseSummary;
export type ServiceAccountSummary = UserBaseSummary;
export type SysUserSummary = UserBaseSummary;

export interface OrgScheduleFragmentsGetResponse {
  org_schedule_fragment: OrgScheduleFragment;
}
export interface AtomBase {
  created_by?: UserSummary;
  /**
   * Timestamp when the object was created.
   */
  created_date?: string;
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
  modified_by?: UserSummary;
  /**
   * Timestamp when the object was last modified.
   */
  modified_date?: string;
}
export interface AtomBaseSummary {
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
}
/**
 * An optionally named period on day granularity.
 */
export interface OrgScheduleInterval {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"400"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "artifact_already_attached_to_a_parent",
  "existing_parent": "string",
  "is_same": true
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorBadRequest = ErrorBase &
  (
    | ErrorBadRequestArtifactAlreadyAttachedToAParent
    | ErrorBadRequestBadRequest
    | ErrorBadRequestInvalidApiVersion
    | ErrorBadRequestInvalidEnumValue
    | ErrorBadRequestInvalidField
    | ErrorBadRequestMissingDependency
    | ErrorBadRequestMissingRequiredField
    | ErrorBadRequestParseError
    | ErrorBadRequestStaleSchemaFragments
    | ErrorBadRequestUnexpectedJsonType
    | ErrorBadRequestValueNotPermitted
  );

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorBadRequestArtifactAlreadyAttachedToAParent {
  /**
   * The existing parent attached to the artifact.
   */
  existing_parent: string;
  /**
   * Whether the existing parent is the same as the new parent.
   *
   */
  is_same: boolean;
}
export interface ErrorBadRequestBadRequest {}
export interface ErrorBadRequestInvalidApiVersion {
  /**
   * The provided API version.
   */
  value: string;
}
export interface ErrorBadRequestInvalidEnumValue {
  /**
   * The allowed values for the field.
   */
  allowed_values: string[];
  /**
   * The field whose enum value is invalid.
   */
  field_name: string;
  /**
   * The value that was received.
   */
  value: string;
}
export interface ErrorBadRequestInvalidField {
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestMissingDependency {
  /**
   * The dependent fields.
   */
  dependencies?: ErrorBadRequestMissingDependencyDependency[];
  /**
   * The field on which the value depends.
   */
  dependent_field_name?: string;
  /**
   * The value which needs to be set of the dependent field.
   */
  dependent_field_value?: string;
  /**
   * The field whose value was received.
   */
  provided_field_name?: string;
  /**
   * The value that was received.
   */
  provided_field_value?: string;
}
export interface ErrorBadRequestMissingDependencyDependency {
  /**
   * The dependent field name.
   */
  field_name: string;
  /**
   * The dependent field value.
   */
  field_value: string;
}
export interface ErrorBadRequestMissingRequiredField {
  /**
   * The missing field's name.
   */
  field_name: string;
}
export interface ErrorBadRequestParseError {}
/**
 * Error indicating that the request contained one or more stale schema
 * fragments, which are schema fragments that have been replaced by a
 * newer version and are therefore considered deprecated schema fragments.
 * The caller should refresh and use the latest schema fragments in their
 * request.
 *
 */
export interface ErrorBadRequestStaleSchemaFragments {}
export interface ErrorBadRequestUnexpectedJsonType {
  actual: "array" | "bool" | "null" | "number" | "object" | "string";
  expected: "array" | "bool" | "null" | "number" | "object" | "string";
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestValueNotPermitted {
  /**
   * The allowed values for the field.
   */
  allowed_values?: string[];
  /**
   * The field whose value is not permitted.
   */
  field_name: string;
  /**
   * The reason the value isn't permitted.
   */
  reason?: string;
}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"401"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "unauthenticated"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorUnauthorized = ErrorBase & ErrorUnauthorizedUnauthenticated;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorUnauthorizedUnauthenticated {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"403"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "forbidden"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorForbidden = ErrorBase & ErrorForbiddenForbidden;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorForbiddenForbidden {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"429"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "retry_after": 0,
  "type": "too_many_requests"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorTooManyRequests = ErrorBase & ErrorTooManyRequestsTooManyRequests;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorTooManyRequestsTooManyRequests {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"500"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "reference_id": "string",
  "type": "internal_error"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorInternalServerError = ErrorBase & ErrorInternalServerErrorInternalError;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorInternalServerErrorInternalError {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"503"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "service_unavailable"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorServiceUnavailable = ErrorBase & ErrorServiceUnavailableServiceUnavailable;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorServiceUnavailableServiceUnavailable {}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/org-schedule-fragments.get"}>

## org-schedule-fragments-get-post

Gets an organization schedule fragment.

### Request Body (Optional)



<Property required={true} deprecated={false} name={"id"} type={"string"}>

Organization schedule Fragment ID.

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Success. |
| `400` | 400 Bad Request. |
| `401` | 401 Unauthorized. |
| `403` | 403 Forbidden. |
| `429` | 429 Too Many Requests. |
| `500` | 500 Internal Server Error. |
| `503` | 503 Service Unavailable. |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X POST "https://api.devrev.ai/org-schedule-fragments.get" \
  -d '{
  "id": "string"
}'
```

<Tabs items={["200","400","401","403","429","500","503"]}>

<Tab value={"200"}>



```json title="Example Response"
{
  "org_schedule_fragment": {
    "created_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "created_date": "2023-01-01T12:00:00.000Z",
    "display_id": "string",
    "id": "string",
    "modified_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "modified_date": "2023-01-01T12:00:00.000Z",
    "from": "2023-01-01T12:00:00.000Z",
    "intervals": [
      {}
    ],
    "name": "string",
    "region_codes": [
      "string"
    ],
    "status": "archived",
    "to": "2023-01-01T12:00:00.000Z"
  }
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type OrgScheduleFragment = AtomBase & {
  /**
   * The date (inclusive) on which the organization schedule fragment
   * begins.
   *
   */
  from?: string;
  /**
   * Periods during which the schedule is considered to be 'off' or to
   * be in some specific named period.
   *
   */
  intervals?: OrgScheduleInterval[];
  /**
   * Human-readable name, indicating the purpose of the schedule.
   *
   */
  name?: string;
  /**
   * CLDR region code of the countries/regions it is meant to be valid
   * for. Does not drive logic, serves only for easier filtering and
   * organization.
   *
   */
  region_codes?: string[];
  /**
   * Status determines how an item can be used. In 'draft' status an item
   * can be edited but can't be used. When 'published' the item can longer
   * be edited but can be used. 'Archived' is read-only.
   *
   */
  status: "archived" | "draft" | "published";
  /**
   * The date (exclusive) on which the organization schedule fragment's
   * validity ends.
   *
   */
  to?: string;
};
export type UserSummary = DevUserSummary | RevUserSummary | ServiceAccountSummary | SysUserSummary;
export type DevUserSummary = UserBaseSummary;
export type UserBaseSummary = AtomBaseSummary & {
  /**
   * The user's display name. The name is non-unique and mutable.
   *
   */
  display_name?: string;
  display_picture?: ArtifactSummary;
  /**
   * Email address of the user.
   */
  email?: string;
  /**
   * Full name of the user.
   */
  full_name?: string;
  /**
   * State of the user.
   */
  state?: "active" | "deactivated" | "deleted" | "locked" | "shadow" | "unassigned";
};
export type ArtifactSummary = AtomBaseSummary;
export type RevUserSummary = UserBaseSummary & {
  /**
   * External ref is a mutable unique identifier for a user within the
   * Rev organization from your primary customer record. If none is
   * available, a good alternative is the email address/phone number
   * which could uniquely identify the user. If none is specified, a
   * system-generated identifier will be assigned to the user.
   *
   */
  external_ref?: string;
  rev_org?: OrgSummary;
};
export type OrgSummary = AccountSummary | RevOrgSummary;
export type AccountSummary = OrgBaseSummary;
export type OrgBaseSummary = AtomBaseSummary & {
  /**
   * Name of the Organization.
   */
  display_name?: string;
};
export type RevOrgSummary = OrgBaseSummary;
export type ServiceAccountSummary = UserBaseSummary;
export type SysUserSummary = UserBaseSummary;

export interface OrgScheduleFragmentsGetResponse {
  org_schedule_fragment: OrgScheduleFragment;
}
export interface AtomBase {
  created_by?: UserSummary;
  /**
   * Timestamp when the object was created.
   */
  created_date?: string;
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
  modified_by?: UserSummary;
  /**
   * Timestamp when the object was last modified.
   */
  modified_date?: string;
}
export interface AtomBaseSummary {
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
}
/**
 * An optionally named period on day granularity.
 */
export interface OrgScheduleInterval {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"400"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "artifact_already_attached_to_a_parent",
  "existing_parent": "string",
  "is_same": true
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorBadRequest = ErrorBase &
  (
    | ErrorBadRequestArtifactAlreadyAttachedToAParent
    | ErrorBadRequestBadRequest
    | ErrorBadRequestInvalidApiVersion
    | ErrorBadRequestInvalidEnumValue
    | ErrorBadRequestInvalidField
    | ErrorBadRequestMissingDependency
    | ErrorBadRequestMissingRequiredField
    | ErrorBadRequestParseError
    | ErrorBadRequestStaleSchemaFragments
    | ErrorBadRequestUnexpectedJsonType
    | ErrorBadRequestValueNotPermitted
  );

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorBadRequestArtifactAlreadyAttachedToAParent {
  /**
   * The existing parent attached to the artifact.
   */
  existing_parent: string;
  /**
   * Whether the existing parent is the same as the new parent.
   *
   */
  is_same: boolean;
}
export interface ErrorBadRequestBadRequest {}
export interface ErrorBadRequestInvalidApiVersion {
  /**
   * The provided API version.
   */
  value: string;
}
export interface ErrorBadRequestInvalidEnumValue {
  /**
   * The allowed values for the field.
   */
  allowed_values: string[];
  /**
   * The field whose enum value is invalid.
   */
  field_name: string;
  /**
   * The value that was received.
   */
  value: string;
}
export interface ErrorBadRequestInvalidField {
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestMissingDependency {
  /**
   * The dependent fields.
   */
  dependencies?: ErrorBadRequestMissingDependencyDependency[];
  /**
   * The field on which the value depends.
   */
  dependent_field_name?: string;
  /**
   * The value which needs to be set of the dependent field.
   */
  dependent_field_value?: string;
  /**
   * The field whose value was received.
   */
  provided_field_name?: string;
  /**
   * The value that was received.
   */
  provided_field_value?: string;
}
export interface ErrorBadRequestMissingDependencyDependency {
  /**
   * The dependent field name.
   */
  field_name: string;
  /**
   * The dependent field value.
   */
  field_value: string;
}
export interface ErrorBadRequestMissingRequiredField {
  /**
   * The missing field's name.
   */
  field_name: string;
}
export interface ErrorBadRequestParseError {}
/**
 * Error indicating that the request contained one or more stale schema
 * fragments, which are schema fragments that have been replaced by a
 * newer version and are therefore considered deprecated schema fragments.
 * The caller should refresh and use the latest schema fragments in their
 * request.
 *
 */
export interface ErrorBadRequestStaleSchemaFragments {}
export interface ErrorBadRequestUnexpectedJsonType {
  actual: "array" | "bool" | "null" | "number" | "object" | "string";
  expected: "array" | "bool" | "null" | "number" | "object" | "string";
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestValueNotPermitted {
  /**
   * The allowed values for the field.
   */
  allowed_values?: string[];
  /**
   * The field whose value is not permitted.
   */
  field_name: string;
  /**
   * The reason the value isn't permitted.
   */
  reason?: string;
}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"401"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "unauthenticated"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorUnauthorized = ErrorBase & ErrorUnauthorizedUnauthenticated;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorUnauthorizedUnauthenticated {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"403"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "forbidden"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorForbidden = ErrorBase & ErrorForbiddenForbidden;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorForbiddenForbidden {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"429"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "retry_after": 0,
  "type": "too_many_requests"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorTooManyRequests = ErrorBase & ErrorTooManyRequestsTooManyRequests;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorTooManyRequestsTooManyRequests {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"500"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "reference_id": "string",
  "type": "internal_error"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorInternalServerError = ErrorBase & ErrorInternalServerErrorInternalError;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorInternalServerErrorInternalError {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"503"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "service_unavailable"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorServiceUnavailable = ErrorBase & ErrorServiceUnavailableServiceUnavailable;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorServiceUnavailableServiceUnavailable {}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/org-schedule-fragments.transition"}>

## org-schedule-fragments-transition

Changes stage of an organization schedule fragment.

### Request Body (Optional)



<Property required={true} deprecated={false} name={"id"} type={"string"}>

Organization schedule Fragment ID.

</Property>

<Property required={true} deprecated={false} name={"status"} type={"string"}>

Status determines how an item can be used. In 'draft' status an item
can be edited but can't be used. When 'published' the item can longer
be edited but can be used. 'Archived' is read-only.


<span>Value in:  `"archived" | "draft" | "published"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Success. |
| `400` | 400 Bad Request. |
| `401` | 401 Unauthorized. |
| `403` | 403 Forbidden. |
| `429` | 429 Too Many Requests. |
| `500` | 500 Internal Server Error. |
| `503` | 503 Service Unavailable. |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X POST "https://api.devrev.ai/org-schedule-fragments.transition" \
  -d '{
  "id": "string",
  "status": "archived"
}'
```

<Tabs items={["200","400","401","403","429","500","503"]}>

<Tab value={"200"}>



```json title="Example Response"
{
  "org_schedule_fragment": {
    "created_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "created_date": "2023-01-01T12:00:00.000Z",
    "display_id": "string",
    "id": "string",
    "modified_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "modified_date": "2023-01-01T12:00:00.000Z",
    "from": "2023-01-01T12:00:00.000Z",
    "intervals": [
      {}
    ],
    "name": "string",
    "region_codes": [
      "string"
    ],
    "status": "archived",
    "to": "2023-01-01T12:00:00.000Z"
  }
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type OrgScheduleFragment = AtomBase & {
  /**
   * The date (inclusive) on which the organization schedule fragment
   * begins.
   *
   */
  from?: string;
  /**
   * Periods during which the schedule is considered to be 'off' or to
   * be in some specific named period.
   *
   */
  intervals?: OrgScheduleInterval[];
  /**
   * Human-readable name, indicating the purpose of the schedule.
   *
   */
  name?: string;
  /**
   * CLDR region code of the countries/regions it is meant to be valid
   * for. Does not drive logic, serves only for easier filtering and
   * organization.
   *
   */
  region_codes?: string[];
  /**
   * Status determines how an item can be used. In 'draft' status an item
   * can be edited but can't be used. When 'published' the item can longer
   * be edited but can be used. 'Archived' is read-only.
   *
   */
  status: "archived" | "draft" | "published";
  /**
   * The date (exclusive) on which the organization schedule fragment's
   * validity ends.
   *
   */
  to?: string;
};
export type UserSummary = DevUserSummary | RevUserSummary | ServiceAccountSummary | SysUserSummary;
export type DevUserSummary = UserBaseSummary;
export type UserBaseSummary = AtomBaseSummary & {
  /**
   * The user's display name. The name is non-unique and mutable.
   *
   */
  display_name?: string;
  display_picture?: ArtifactSummary;
  /**
   * Email address of the user.
   */
  email?: string;
  /**
   * Full name of the user.
   */
  full_name?: string;
  /**
   * State of the user.
   */
  state?: "active" | "deactivated" | "deleted" | "locked" | "shadow" | "unassigned";
};
export type ArtifactSummary = AtomBaseSummary;
export type RevUserSummary = UserBaseSummary & {
  /**
   * External ref is a mutable unique identifier for a user within the
   * Rev organization from your primary customer record. If none is
   * available, a good alternative is the email address/phone number
   * which could uniquely identify the user. If none is specified, a
   * system-generated identifier will be assigned to the user.
   *
   */
  external_ref?: string;
  rev_org?: OrgSummary;
};
export type OrgSummary = AccountSummary | RevOrgSummary;
export type AccountSummary = OrgBaseSummary;
export type OrgBaseSummary = AtomBaseSummary & {
  /**
   * Name of the Organization.
   */
  display_name?: string;
};
export type RevOrgSummary = OrgBaseSummary;
export type ServiceAccountSummary = UserBaseSummary;
export type SysUserSummary = UserBaseSummary;

export interface OrgScheduleFragmentsTransitionResponse {
  org_schedule_fragment: OrgScheduleFragment;
}
export interface AtomBase {
  created_by?: UserSummary;
  /**
   * Timestamp when the object was created.
   */
  created_date?: string;
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
  modified_by?: UserSummary;
  /**
   * Timestamp when the object was last modified.
   */
  modified_date?: string;
}
export interface AtomBaseSummary {
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
}
/**
 * An optionally named period on day granularity.
 */
export interface OrgScheduleInterval {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"400"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "artifact_already_attached_to_a_parent",
  "existing_parent": "string",
  "is_same": true
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorBadRequest = ErrorBase &
  (
    | ErrorBadRequestArtifactAlreadyAttachedToAParent
    | ErrorBadRequestBadRequest
    | ErrorBadRequestInvalidApiVersion
    | ErrorBadRequestInvalidEnumValue
    | ErrorBadRequestInvalidField
    | ErrorBadRequestMissingDependency
    | ErrorBadRequestMissingRequiredField
    | ErrorBadRequestParseError
    | ErrorBadRequestStaleSchemaFragments
    | ErrorBadRequestUnexpectedJsonType
    | ErrorBadRequestValueNotPermitted
  );

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorBadRequestArtifactAlreadyAttachedToAParent {
  /**
   * The existing parent attached to the artifact.
   */
  existing_parent: string;
  /**
   * Whether the existing parent is the same as the new parent.
   *
   */
  is_same: boolean;
}
export interface ErrorBadRequestBadRequest {}
export interface ErrorBadRequestInvalidApiVersion {
  /**
   * The provided API version.
   */
  value: string;
}
export interface ErrorBadRequestInvalidEnumValue {
  /**
   * The allowed values for the field.
   */
  allowed_values: string[];
  /**
   * The field whose enum value is invalid.
   */
  field_name: string;
  /**
   * The value that was received.
   */
  value: string;
}
export interface ErrorBadRequestInvalidField {
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestMissingDependency {
  /**
   * The dependent fields.
   */
  dependencies?: ErrorBadRequestMissingDependencyDependency[];
  /**
   * The field on which the value depends.
   */
  dependent_field_name?: string;
  /**
   * The value which needs to be set of the dependent field.
   */
  dependent_field_value?: string;
  /**
   * The field whose value was received.
   */
  provided_field_name?: string;
  /**
   * The value that was received.
   */
  provided_field_value?: string;
}
export interface ErrorBadRequestMissingDependencyDependency {
  /**
   * The dependent field name.
   */
  field_name: string;
  /**
   * The dependent field value.
   */
  field_value: string;
}
export interface ErrorBadRequestMissingRequiredField {
  /**
   * The missing field's name.
   */
  field_name: string;
}
export interface ErrorBadRequestParseError {}
/**
 * Error indicating that the request contained one or more stale schema
 * fragments, which are schema fragments that have been replaced by a
 * newer version and are therefore considered deprecated schema fragments.
 * The caller should refresh and use the latest schema fragments in their
 * request.
 *
 */
export interface ErrorBadRequestStaleSchemaFragments {}
export interface ErrorBadRequestUnexpectedJsonType {
  actual: "array" | "bool" | "null" | "number" | "object" | "string";
  expected: "array" | "bool" | "null" | "number" | "object" | "string";
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestValueNotPermitted {
  /**
   * The allowed values for the field.
   */
  allowed_values?: string[];
  /**
   * The field whose value is not permitted.
   */
  field_name: string;
  /**
   * The reason the value isn't permitted.
   */
  reason?: string;
}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"401"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "unauthenticated"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorUnauthorized = ErrorBase & ErrorUnauthorizedUnauthenticated;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorUnauthorizedUnauthenticated {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"403"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "forbidden"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorForbidden = ErrorBase & ErrorForbiddenForbidden;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorForbiddenForbidden {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"429"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "retry_after": 0,
  "type": "too_many_requests"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorTooManyRequests = ErrorBase & ErrorTooManyRequestsTooManyRequests;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorTooManyRequestsTooManyRequests {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"500"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "reference_id": "string",
  "type": "internal_error"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorInternalServerError = ErrorBase & ErrorInternalServerErrorInternalError;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorInternalServerErrorInternalError {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"503"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "service_unavailable"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorServiceUnavailable = ErrorBase & ErrorServiceUnavailableServiceUnavailable;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorServiceUnavailableServiceUnavailable {}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/org-schedules.create"}>

## org-schedules-create

Creates an organization schedule with a default weekly organization
schedule and a list of organization schedule fragments.


### Request Body (Optional)



<Property required={false} deprecated={false} name={"default_weekly_org_schedule"} type={"object"}>



<Accordions>

<Accordion title={"Object Type"}>

<Property required={true} deprecated={false} name={"intervals"} type={"array of object"}>

The 'on' intervals of the week.

<Accordions>

<Accordion title={"Object Type"}>

<Property required={true} deprecated={false} name={"from"} type={"integer"}>

Duration in minutes of the week when the interval starts. 0 is
Sunday midnight, when Sunday ends and Monday begins.


<span>Format:  `"int64"`</span>

</Property>

<Property required={true} deprecated={false} name={"to"} type={"integer"}>

Duration in minutes of the week when the interval ends (must be
larger than 'from'). 0 is Sunday midnight, when Sunday ends and
Monday begins.


<span>Format:  `"int64"`</span>

</Property>

</Accordion>

</Accordions>

</Property>

<Property required={true} deprecated={false} name={"period_name"} type={"string"}>

The name of the period during which the organization schedule
applies.


</Property>

</Accordion>

</Accordions>

</Property>

<Property required={false} deprecated={false} name={"min_valid_days"} type={"integer"}>

Organization schedule must be valid for at least this many days in
the future. Meaning organization schedule fragments must cover this
period. 0 if omitted.


<span>Format:  `"int64"`</span>

</Property>

<Property required={true} deprecated={false} name={"name"} type={"string"}>

Human-readable name.

</Property>

<Property required={false} deprecated={false} name={"org_schedule_fragments"} type={"array of object"}>

List of organization schedule fragments with no overlaps or gaps.


<Accordions>

<Accordion title={"Object Type"}>

<Property required={true} deprecated={false} name={"id"} type={"string"}>

Organization schedule fragment ID.

</Property>

</Accordion>

</Accordions>

</Property>

<Property required={true} deprecated={false} name={"timezone"} type={"string"}>

Timezone in which the organization schedule applies. Expected to be
a valid IANA time zone name such as America/New_York.


</Property>

<Property required={false} deprecated={false} name={"weekly_org_schedules"} type={"array of object"}>

If this day belongs to a named period according to the currently
active organization schedule fragment, a weekly organization
schedule from this list with the corresponding name will apply.


<Accordions>

<Accordion title={"Object Type"}>

<Property required={true} deprecated={false} name={"intervals"} type={"array of object"}>

The 'on' intervals of the week.

<Accordions>

<Accordion title={"Object Type"}>

<Property required={true} deprecated={false} name={"from"} type={"integer"}>

Duration in minutes of the week when the interval starts. 0 is
Sunday midnight, when Sunday ends and Monday begins.


<span>Format:  `"int64"`</span>

</Property>

<Property required={true} deprecated={false} name={"to"} type={"integer"}>

Duration in minutes of the week when the interval ends (must be
larger than 'from'). 0 is Sunday midnight, when Sunday ends and
Monday begins.


<span>Format:  `"int64"`</span>

</Property>

</Accordion>

</Accordions>

</Property>

<Property required={true} deprecated={false} name={"period_name"} type={"string"}>

The name of the period during which the organization schedule
applies.


</Property>

</Accordion>

</Accordions>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `201` | Success. |
| `400` | 400 Bad Request. |
| `401` | 401 Unauthorized. |
| `403` | 403 Forbidden. |
| `429` | 429 Too Many Requests. |
| `500` | 500 Internal Server Error. |
| `503` | 503 Service Unavailable. |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X POST "https://api.devrev.ai/org-schedules.create" \
  -d '{
  "default_weekly_org_schedule": {
    "intervals": [
      {
        "from": 0,
        "to": 0
      }
    ],
    "period_name": "string"
  },
  "min_valid_days": 0,
  "name": "string",
  "org_schedule_fragments": [
    {
      "id": "string"
    }
  ],
  "timezone": "string",
  "weekly_org_schedules": [
    {
      "intervals": [
        {
          "from": 0,
          "to": 0
        }
      ],
      "period_name": "string"
    }
  ]
}'
```

<Tabs items={["201","400","401","403","429","500","503"]}>

<Tab value={"201"}>



```json title="Example Response"
{
  "org_schedule": {
    "created_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "created_date": "2023-01-01T12:00:00.000Z",
    "display_id": "string",
    "id": "string",
    "modified_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "modified_date": "2023-01-01T12:00:00.000Z",
    "default_weekly_org_schedule": {},
    "min_valid_days": 0,
    "name": "string",
    "org_schedule_fragments": [
      {}
    ],
    "status": "archived",
    "timezone": "string",
    "valid_until": "2023-01-01T12:00:00.000Z",
    "weekly_org_schedules": [
      {}
    ]
  }
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type OrgSchedule = AtomBase & {
  default_weekly_org_schedule?: WeeklyOrgSchedule;
  /**
   * The schedule must be valid and well-defined for at least this many
   * days in the future, otherwise a warning notification is generated.
   * Default is 0 if not specified.
   *
   */
  min_valid_days?: number;
  /**
   * Human-readable name.
   */
  name?: string;
  /**
   * The list of schedule fragments. It must be an ordered list of
   * contiguous fragments (the next starting when the previous one
   * ends), updates in a published schedule are only allowed to add new
   * ones to the future.
   *
   */
  org_schedule_fragments?: OrgScheduleFragmentOverview[];
  /**
   * Status determines how an item can be used. In 'draft' status an item
   * can be edited but can't be used. When 'published' the item can longer
   * be edited but can be used. 'Archived' is read-only.
   *
   */
  status: "archived" | "draft" | "published";
  /**
   * Timezone in which this is defined. Only organization schedules in
   * the same timezone can be directly combined.
   *
   */
  timezone?: string;
  /**
   * Derived field indicating when a valid organization schedule will
   * become invalid. If omitted, the schedule is already invalid. A
   * schedule is valid if it has a weekly schedule for all named periods
   * for all its schedule fragments, and if it has a schedule fragment
   * for the time period in question.
   *
   */
  valid_until?: string;
  /**
   * If the organization schedule fragment specifies that the given day
   * belongs to a named period, a weekly schedule from this list with
   * the matching name will be selected.
   *
   */
  weekly_org_schedules?: WeeklyOrgSchedule[];
};
export type UserSummary = DevUserSummary | RevUserSummary | ServiceAccountSummary | SysUserSummary;
export type DevUserSummary = UserBaseSummary;
export type UserBaseSummary = AtomBaseSummary & {
  /**
   * The user's display name. The name is non-unique and mutable.
   *
   */
  display_name?: string;
  display_picture?: ArtifactSummary;
  /**
   * Email address of the user.
   */
  email?: string;
  /**
   * Full name of the user.
   */
  full_name?: string;
  /**
   * State of the user.
   */
  state?: "active" | "deactivated" | "deleted" | "locked" | "shadow" | "unassigned";
};
export type ArtifactSummary = AtomBaseSummary;
export type RevUserSummary = UserBaseSummary & {
  /**
   * External ref is a mutable unique identifier for a user within the
   * Rev organization from your primary customer record. If none is
   * available, a good alternative is the email address/phone number
   * which could uniquely identify the user. If none is specified, a
   * system-generated identifier will be assigned to the user.
   *
   */
  external_ref?: string;
  rev_org?: OrgSummary;
};
export type OrgSummary = AccountSummary | RevOrgSummary;
export type AccountSummary = OrgBaseSummary;
export type OrgBaseSummary = AtomBaseSummary & {
  /**
   * Name of the Organization.
   */
  display_name?: string;
};
export type RevOrgSummary = OrgBaseSummary;
export type ServiceAccountSummary = UserBaseSummary;
export type SysUserSummary = UserBaseSummary;

export interface OrgSchedulesCreateResponse {
  org_schedule: OrgSchedule;
}
export interface AtomBase {
  created_by?: UserSummary;
  /**
   * Timestamp when the object was created.
   */
  created_date?: string;
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
  modified_by?: UserSummary;
  /**
   * Timestamp when the object was last modified.
   */
  modified_date?: string;
}
export interface AtomBaseSummary {
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
}
/**
 * The schedule for each week.
 */
export interface WeeklyOrgSchedule {}
/**
 * The representation of the organization schedule fragment embedded
 * inside a organization schedule, used to quickly look up the schedule
 * fragment for the right period.
 *
 */
export interface OrgScheduleFragmentOverview {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"400"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "artifact_already_attached_to_a_parent",
  "existing_parent": "string",
  "is_same": true
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorBadRequest = ErrorBase &
  (
    | ErrorBadRequestArtifactAlreadyAttachedToAParent
    | ErrorBadRequestBadRequest
    | ErrorBadRequestInvalidApiVersion
    | ErrorBadRequestInvalidEnumValue
    | ErrorBadRequestInvalidField
    | ErrorBadRequestMissingDependency
    | ErrorBadRequestMissingRequiredField
    | ErrorBadRequestParseError
    | ErrorBadRequestStaleSchemaFragments
    | ErrorBadRequestUnexpectedJsonType
    | ErrorBadRequestValueNotPermitted
  );

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorBadRequestArtifactAlreadyAttachedToAParent {
  /**
   * The existing parent attached to the artifact.
   */
  existing_parent: string;
  /**
   * Whether the existing parent is the same as the new parent.
   *
   */
  is_same: boolean;
}
export interface ErrorBadRequestBadRequest {}
export interface ErrorBadRequestInvalidApiVersion {
  /**
   * The provided API version.
   */
  value: string;
}
export interface ErrorBadRequestInvalidEnumValue {
  /**
   * The allowed values for the field.
   */
  allowed_values: string[];
  /**
   * The field whose enum value is invalid.
   */
  field_name: string;
  /**
   * The value that was received.
   */
  value: string;
}
export interface ErrorBadRequestInvalidField {
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestMissingDependency {
  /**
   * The dependent fields.
   */
  dependencies?: ErrorBadRequestMissingDependencyDependency[];
  /**
   * The field on which the value depends.
   */
  dependent_field_name?: string;
  /**
   * The value which needs to be set of the dependent field.
   */
  dependent_field_value?: string;
  /**
   * The field whose value was received.
   */
  provided_field_name?: string;
  /**
   * The value that was received.
   */
  provided_field_value?: string;
}
export interface ErrorBadRequestMissingDependencyDependency {
  /**
   * The dependent field name.
   */
  field_name: string;
  /**
   * The dependent field value.
   */
  field_value: string;
}
export interface ErrorBadRequestMissingRequiredField {
  /**
   * The missing field's name.
   */
  field_name: string;
}
export interface ErrorBadRequestParseError {}
/**
 * Error indicating that the request contained one or more stale schema
 * fragments, which are schema fragments that have been replaced by a
 * newer version and are therefore considered deprecated schema fragments.
 * The caller should refresh and use the latest schema fragments in their
 * request.
 *
 */
export interface ErrorBadRequestStaleSchemaFragments {}
export interface ErrorBadRequestUnexpectedJsonType {
  actual: "array" | "bool" | "null" | "number" | "object" | "string";
  expected: "array" | "bool" | "null" | "number" | "object" | "string";
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestValueNotPermitted {
  /**
   * The allowed values for the field.
   */
  allowed_values?: string[];
  /**
   * The field whose value is not permitted.
   */
  field_name: string;
  /**
   * The reason the value isn't permitted.
   */
  reason?: string;
}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"401"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "unauthenticated"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorUnauthorized = ErrorBase & ErrorUnauthorizedUnauthenticated;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorUnauthorizedUnauthenticated {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"403"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "forbidden"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorForbidden = ErrorBase & ErrorForbiddenForbidden;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorForbiddenForbidden {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"429"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "retry_after": 0,
  "type": "too_many_requests"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorTooManyRequests = ErrorBase & ErrorTooManyRequestsTooManyRequests;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorTooManyRequestsTooManyRequests {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"500"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "reference_id": "string",
  "type": "internal_error"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorInternalServerError = ErrorBase & ErrorInternalServerErrorInternalError;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorInternalServerErrorInternalError {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"503"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "service_unavailable"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorServiceUnavailable = ErrorBase & ErrorServiceUnavailableServiceUnavailable;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorServiceUnavailableServiceUnavailable {}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"GET"} route={"/org-schedules.get"}>

## org-schedules-get

Gets an organization schedule.

### Query Parameters

<Property required={true} deprecated={false} name={"id"} type={"string"}>

Organization schedule ID.

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Success. |
| `400` | 400 Bad Request. |
| `401` | 401 Unauthorized. |
| `403` | 403 Forbidden. |
| `429` | 429 Too Many Requests. |
| `500` | 500 Internal Server Error. |
| `503` | 503 Service Unavailable. |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X GET "https://api.devrev.ai/org-schedules.get"
```

<Tabs items={["200","400","401","403","429","500","503"]}>

<Tab value={"200"}>



```json title="Example Response"
{
  "org_schedule": {
    "created_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "created_date": "2023-01-01T12:00:00.000Z",
    "display_id": "string",
    "id": "string",
    "modified_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "modified_date": "2023-01-01T12:00:00.000Z",
    "default_weekly_org_schedule": {},
    "min_valid_days": 0,
    "name": "string",
    "org_schedule_fragments": [
      {}
    ],
    "status": "archived",
    "timezone": "string",
    "valid_until": "2023-01-01T12:00:00.000Z",
    "weekly_org_schedules": [
      {}
    ]
  }
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type OrgSchedule = AtomBase & {
  default_weekly_org_schedule?: WeeklyOrgSchedule;
  /**
   * The schedule must be valid and well-defined for at least this many
   * days in the future, otherwise a warning notification is generated.
   * Default is 0 if not specified.
   *
   */
  min_valid_days?: number;
  /**
   * Human-readable name.
   */
  name?: string;
  /**
   * The list of schedule fragments. It must be an ordered list of
   * contiguous fragments (the next starting when the previous one
   * ends), updates in a published schedule are only allowed to add new
   * ones to the future.
   *
   */
  org_schedule_fragments?: OrgScheduleFragmentOverview[];
  /**
   * Status determines how an item can be used. In 'draft' status an item
   * can be edited but can't be used. When 'published' the item can longer
   * be edited but can be used. 'Archived' is read-only.
   *
   */
  status: "archived" | "draft" | "published";
  /**
   * Timezone in which this is defined. Only organization schedules in
   * the same timezone can be directly combined.
   *
   */
  timezone?: string;
  /**
   * Derived field indicating when a valid organization schedule will
   * become invalid. If omitted, the schedule is already invalid. A
   * schedule is valid if it has a weekly schedule for all named periods
   * for all its schedule fragments, and if it has a schedule fragment
   * for the time period in question.
   *
   */
  valid_until?: string;
  /**
   * If the organization schedule fragment specifies that the given day
   * belongs to a named period, a weekly schedule from this list with
   * the matching name will be selected.
   *
   */
  weekly_org_schedules?: WeeklyOrgSchedule[];
};
export type UserSummary = DevUserSummary | RevUserSummary | ServiceAccountSummary | SysUserSummary;
export type DevUserSummary = UserBaseSummary;
export type UserBaseSummary = AtomBaseSummary & {
  /**
   * The user's display name. The name is non-unique and mutable.
   *
   */
  display_name?: string;
  display_picture?: ArtifactSummary;
  /**
   * Email address of the user.
   */
  email?: string;
  /**
   * Full name of the user.
   */
  full_name?: string;
  /**
   * State of the user.
   */
  state?: "active" | "deactivated" | "deleted" | "locked" | "shadow" | "unassigned";
};
export type ArtifactSummary = AtomBaseSummary;
export type RevUserSummary = UserBaseSummary & {
  /**
   * External ref is a mutable unique identifier for a user within the
   * Rev organization from your primary customer record. If none is
   * available, a good alternative is the email address/phone number
   * which could uniquely identify the user. If none is specified, a
   * system-generated identifier will be assigned to the user.
   *
   */
  external_ref?: string;
  rev_org?: OrgSummary;
};
export type OrgSummary = AccountSummary | RevOrgSummary;
export type AccountSummary = OrgBaseSummary;
export type OrgBaseSummary = AtomBaseSummary & {
  /**
   * Name of the Organization.
   */
  display_name?: string;
};
export type RevOrgSummary = OrgBaseSummary;
export type ServiceAccountSummary = UserBaseSummary;
export type SysUserSummary = UserBaseSummary;

export interface OrgSchedulesGetResponse {
  org_schedule: OrgSchedule;
}
export interface AtomBase {
  created_by?: UserSummary;
  /**
   * Timestamp when the object was created.
   */
  created_date?: string;
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
  modified_by?: UserSummary;
  /**
   * Timestamp when the object was last modified.
   */
  modified_date?: string;
}
export interface AtomBaseSummary {
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
}
/**
 * The schedule for each week.
 */
export interface WeeklyOrgSchedule {}
/**
 * The representation of the organization schedule fragment embedded
 * inside a organization schedule, used to quickly look up the schedule
 * fragment for the right period.
 *
 */
export interface OrgScheduleFragmentOverview {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"400"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "artifact_already_attached_to_a_parent",
  "existing_parent": "string",
  "is_same": true
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorBadRequest = ErrorBase &
  (
    | ErrorBadRequestArtifactAlreadyAttachedToAParent
    | ErrorBadRequestBadRequest
    | ErrorBadRequestInvalidApiVersion
    | ErrorBadRequestInvalidEnumValue
    | ErrorBadRequestInvalidField
    | ErrorBadRequestMissingDependency
    | ErrorBadRequestMissingRequiredField
    | ErrorBadRequestParseError
    | ErrorBadRequestStaleSchemaFragments
    | ErrorBadRequestUnexpectedJsonType
    | ErrorBadRequestValueNotPermitted
  );

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorBadRequestArtifactAlreadyAttachedToAParent {
  /**
   * The existing parent attached to the artifact.
   */
  existing_parent: string;
  /**
   * Whether the existing parent is the same as the new parent.
   *
   */
  is_same: boolean;
}
export interface ErrorBadRequestBadRequest {}
export interface ErrorBadRequestInvalidApiVersion {
  /**
   * The provided API version.
   */
  value: string;
}
export interface ErrorBadRequestInvalidEnumValue {
  /**
   * The allowed values for the field.
   */
  allowed_values: string[];
  /**
   * The field whose enum value is invalid.
   */
  field_name: string;
  /**
   * The value that was received.
   */
  value: string;
}
export interface ErrorBadRequestInvalidField {
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestMissingDependency {
  /**
   * The dependent fields.
   */
  dependencies?: ErrorBadRequestMissingDependencyDependency[];
  /**
   * The field on which the value depends.
   */
  dependent_field_name?: string;
  /**
   * The value which needs to be set of the dependent field.
   */
  dependent_field_value?: string;
  /**
   * The field whose value was received.
   */
  provided_field_name?: string;
  /**
   * The value that was received.
   */
  provided_field_value?: string;
}
export interface ErrorBadRequestMissingDependencyDependency {
  /**
   * The dependent field name.
   */
  field_name: string;
  /**
   * The dependent field value.
   */
  field_value: string;
}
export interface ErrorBadRequestMissingRequiredField {
  /**
   * The missing field's name.
   */
  field_name: string;
}
export interface ErrorBadRequestParseError {}
/**
 * Error indicating that the request contained one or more stale schema
 * fragments, which are schema fragments that have been replaced by a
 * newer version and are therefore considered deprecated schema fragments.
 * The caller should refresh and use the latest schema fragments in their
 * request.
 *
 */
export interface ErrorBadRequestStaleSchemaFragments {}
export interface ErrorBadRequestUnexpectedJsonType {
  actual: "array" | "bool" | "null" | "number" | "object" | "string";
  expected: "array" | "bool" | "null" | "number" | "object" | "string";
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestValueNotPermitted {
  /**
   * The allowed values for the field.
   */
  allowed_values?: string[];
  /**
   * The field whose value is not permitted.
   */
  field_name: string;
  /**
   * The reason the value isn't permitted.
   */
  reason?: string;
}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"401"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "unauthenticated"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorUnauthorized = ErrorBase & ErrorUnauthorizedUnauthenticated;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorUnauthorizedUnauthenticated {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"403"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "forbidden"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorForbidden = ErrorBase & ErrorForbiddenForbidden;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorForbiddenForbidden {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"429"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "retry_after": 0,
  "type": "too_many_requests"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorTooManyRequests = ErrorBase & ErrorTooManyRequestsTooManyRequests;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorTooManyRequestsTooManyRequests {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"500"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "reference_id": "string",
  "type": "internal_error"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorInternalServerError = ErrorBase & ErrorInternalServerErrorInternalError;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorInternalServerErrorInternalError {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"503"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "service_unavailable"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorServiceUnavailable = ErrorBase & ErrorServiceUnavailableServiceUnavailable;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorServiceUnavailableServiceUnavailable {}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/org-schedules.get"}>

## org-schedules-get-post

Gets an organization schedule.

### Request Body (Optional)



<Property required={true} deprecated={false} name={"id"} type={"string"}>

Organization schedule ID.

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Success. |
| `400` | 400 Bad Request. |
| `401` | 401 Unauthorized. |
| `403` | 403 Forbidden. |
| `429` | 429 Too Many Requests. |
| `500` | 500 Internal Server Error. |
| `503` | 503 Service Unavailable. |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X POST "https://api.devrev.ai/org-schedules.get" \
  -d '{
  "id": "string"
}'
```

<Tabs items={["200","400","401","403","429","500","503"]}>

<Tab value={"200"}>



```json title="Example Response"
{
  "org_schedule": {
    "created_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "created_date": "2023-01-01T12:00:00.000Z",
    "display_id": "string",
    "id": "string",
    "modified_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "modified_date": "2023-01-01T12:00:00.000Z",
    "default_weekly_org_schedule": {},
    "min_valid_days": 0,
    "name": "string",
    "org_schedule_fragments": [
      {}
    ],
    "status": "archived",
    "timezone": "string",
    "valid_until": "2023-01-01T12:00:00.000Z",
    "weekly_org_schedules": [
      {}
    ]
  }
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type OrgSchedule = AtomBase & {
  default_weekly_org_schedule?: WeeklyOrgSchedule;
  /**
   * The schedule must be valid and well-defined for at least this many
   * days in the future, otherwise a warning notification is generated.
   * Default is 0 if not specified.
   *
   */
  min_valid_days?: number;
  /**
   * Human-readable name.
   */
  name?: string;
  /**
   * The list of schedule fragments. It must be an ordered list of
   * contiguous fragments (the next starting when the previous one
   * ends), updates in a published schedule are only allowed to add new
   * ones to the future.
   *
   */
  org_schedule_fragments?: OrgScheduleFragmentOverview[];
  /**
   * Status determines how an item can be used. In 'draft' status an item
   * can be edited but can't be used. When 'published' the item can longer
   * be edited but can be used. 'Archived' is read-only.
   *
   */
  status: "archived" | "draft" | "published";
  /**
   * Timezone in which this is defined. Only organization schedules in
   * the same timezone can be directly combined.
   *
   */
  timezone?: string;
  /**
   * Derived field indicating when a valid organization schedule will
   * become invalid. If omitted, the schedule is already invalid. A
   * schedule is valid if it has a weekly schedule for all named periods
   * for all its schedule fragments, and if it has a schedule fragment
   * for the time period in question.
   *
   */
  valid_until?: string;
  /**
   * If the organization schedule fragment specifies that the given day
   * belongs to a named period, a weekly schedule from this list with
   * the matching name will be selected.
   *
   */
  weekly_org_schedules?: WeeklyOrgSchedule[];
};
export type UserSummary = DevUserSummary | RevUserSummary | ServiceAccountSummary | SysUserSummary;
export type DevUserSummary = UserBaseSummary;
export type UserBaseSummary = AtomBaseSummary & {
  /**
   * The user's display name. The name is non-unique and mutable.
   *
   */
  display_name?: string;
  display_picture?: ArtifactSummary;
  /**
   * Email address of the user.
   */
  email?: string;
  /**
   * Full name of the user.
   */
  full_name?: string;
  /**
   * State of the user.
   */
  state?: "active" | "deactivated" | "deleted" | "locked" | "shadow" | "unassigned";
};
export type ArtifactSummary = AtomBaseSummary;
export type RevUserSummary = UserBaseSummary & {
  /**
   * External ref is a mutable unique identifier for a user within the
   * Rev organization from your primary customer record. If none is
   * available, a good alternative is the email address/phone number
   * which could uniquely identify the user. If none is specified, a
   * system-generated identifier will be assigned to the user.
   *
   */
  external_ref?: string;
  rev_org?: OrgSummary;
};
export type OrgSummary = AccountSummary | RevOrgSummary;
export type AccountSummary = OrgBaseSummary;
export type OrgBaseSummary = AtomBaseSummary & {
  /**
   * Name of the Organization.
   */
  display_name?: string;
};
export type RevOrgSummary = OrgBaseSummary;
export type ServiceAccountSummary = UserBaseSummary;
export type SysUserSummary = UserBaseSummary;

export interface OrgSchedulesGetResponse {
  org_schedule: OrgSchedule;
}
export interface AtomBase {
  created_by?: UserSummary;
  /**
   * Timestamp when the object was created.
   */
  created_date?: string;
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
  modified_by?: UserSummary;
  /**
   * Timestamp when the object was last modified.
   */
  modified_date?: string;
}
export interface AtomBaseSummary {
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
}
/**
 * The schedule for each week.
 */
export interface WeeklyOrgSchedule {}
/**
 * The representation of the organization schedule fragment embedded
 * inside a organization schedule, used to quickly look up the schedule
 * fragment for the right period.
 *
 */
export interface OrgScheduleFragmentOverview {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"400"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "artifact_already_attached_to_a_parent",
  "existing_parent": "string",
  "is_same": true
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorBadRequest = ErrorBase &
  (
    | ErrorBadRequestArtifactAlreadyAttachedToAParent
    | ErrorBadRequestBadRequest
    | ErrorBadRequestInvalidApiVersion
    | ErrorBadRequestInvalidEnumValue
    | ErrorBadRequestInvalidField
    | ErrorBadRequestMissingDependency
    | ErrorBadRequestMissingRequiredField
    | ErrorBadRequestParseError
    | ErrorBadRequestStaleSchemaFragments
    | ErrorBadRequestUnexpectedJsonType
    | ErrorBadRequestValueNotPermitted
  );

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorBadRequestArtifactAlreadyAttachedToAParent {
  /**
   * The existing parent attached to the artifact.
   */
  existing_parent: string;
  /**
   * Whether the existing parent is the same as the new parent.
   *
   */
  is_same: boolean;
}
export interface ErrorBadRequestBadRequest {}
export interface ErrorBadRequestInvalidApiVersion {
  /**
   * The provided API version.
   */
  value: string;
}
export interface ErrorBadRequestInvalidEnumValue {
  /**
   * The allowed values for the field.
   */
  allowed_values: string[];
  /**
   * The field whose enum value is invalid.
   */
  field_name: string;
  /**
   * The value that was received.
   */
  value: string;
}
export interface ErrorBadRequestInvalidField {
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestMissingDependency {
  /**
   * The dependent fields.
   */
  dependencies?: ErrorBadRequestMissingDependencyDependency[];
  /**
   * The field on which the value depends.
   */
  dependent_field_name?: string;
  /**
   * The value which needs to be set of the dependent field.
   */
  dependent_field_value?: string;
  /**
   * The field whose value was received.
   */
  provided_field_name?: string;
  /**
   * The value that was received.
   */
  provided_field_value?: string;
}
export interface ErrorBadRequestMissingDependencyDependency {
  /**
   * The dependent field name.
   */
  field_name: string;
  /**
   * The dependent field value.
   */
  field_value: string;
}
export interface ErrorBadRequestMissingRequiredField {
  /**
   * The missing field's name.
   */
  field_name: string;
}
export interface ErrorBadRequestParseError {}
/**
 * Error indicating that the request contained one or more stale schema
 * fragments, which are schema fragments that have been replaced by a
 * newer version and are therefore considered deprecated schema fragments.
 * The caller should refresh and use the latest schema fragments in their
 * request.
 *
 */
export interface ErrorBadRequestStaleSchemaFragments {}
export interface ErrorBadRequestUnexpectedJsonType {
  actual: "array" | "bool" | "null" | "number" | "object" | "string";
  expected: "array" | "bool" | "null" | "number" | "object" | "string";
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestValueNotPermitted {
  /**
   * The allowed values for the field.
   */
  allowed_values?: string[];
  /**
   * The field whose value is not permitted.
   */
  field_name: string;
  /**
   * The reason the value isn't permitted.
   */
  reason?: string;
}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"401"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "unauthenticated"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorUnauthorized = ErrorBase & ErrorUnauthorizedUnauthenticated;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorUnauthorizedUnauthenticated {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"403"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "forbidden"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorForbidden = ErrorBase & ErrorForbiddenForbidden;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorForbiddenForbidden {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"429"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "retry_after": 0,
  "type": "too_many_requests"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorTooManyRequests = ErrorBase & ErrorTooManyRequestsTooManyRequests;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorTooManyRequestsTooManyRequests {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"500"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "reference_id": "string",
  "type": "internal_error"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorInternalServerError = ErrorBase & ErrorInternalServerErrorInternalError;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorInternalServerErrorInternalError {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"503"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "service_unavailable"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorServiceUnavailable = ErrorBase & ErrorServiceUnavailableServiceUnavailable;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorServiceUnavailableServiceUnavailable {}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"GET"} route={"/org-schedules.list"}>

## org-schedules-list

Gets list of organization schedules.

### Query Parameters

<Property required={false} deprecated={false} name={"created_by_id"} type={"array of string"}>

Creator ID the filter matches.

</Property>

<Property required={false} deprecated={false} name={"cursor"} type={"string"}>

The cursor to resume iteration from. If not provided, then iteration
starts from the beginning.


</Property>

<Property required={false} deprecated={false} name={"limit"} type={"integer"}>

Max number of organization schedules returned in a page. Default is
50.


<span>Format:  `"int32"`</span>

</Property>

<Property required={false} deprecated={false} name={"mode"} type={"string"}>

The iteration mode to use, otherwise if not set, then "after" is
used.


<span>Value in:  `"after" | "before"`</span>

</Property>

<Property required={false} deprecated={false} name={"status"} type={"array of string"}>

The organization schedule statuses the filter matches.

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Success. |
| `400` | 400 Bad Request. |
| `401` | 401 Unauthorized. |
| `403` | 403 Forbidden. |
| `429` | 429 Too Many Requests. |
| `500` | 500 Internal Server Error. |
| `503` | 503 Service Unavailable. |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X GET "https://api.devrev.ai/org-schedules.list"
```

<Tabs items={["200","400","401","403","429","500","503"]}>

<Tab value={"200"}>



```json title="Example Response"
{
  "next_cursor": "string",
  "org_schedules": [
    {
      "created_by": {
        "type": "dev_user",
        "display_id": "string",
        "id": "string",
        "display_name": "string",
        "display_picture": {
          "display_id": "string",
          "id": "string"
        },
        "email": "string",
        "full_name": "string",
        "state": "active"
      },
      "created_date": "2023-01-01T12:00:00.000Z",
      "display_id": "string",
      "id": "string",
      "modified_by": {
        "type": "dev_user",
        "display_id": "string",
        "id": "string",
        "display_name": "string",
        "display_picture": {
          "display_id": "string",
          "id": "string"
        },
        "email": "string",
        "full_name": "string",
        "state": "active"
      },
      "modified_date": "2023-01-01T12:00:00.000Z",
      "default_weekly_org_schedule": {},
      "min_valid_days": 0,
      "name": "string",
      "org_schedule_fragments": [
        {}
      ],
      "status": "archived",
      "timezone": "string",
      "valid_until": "2023-01-01T12:00:00.000Z",
      "weekly_org_schedules": [
        {}
      ]
    }
  ],
  "prev_cursor": "string"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type OrgSchedule = AtomBase & {
  default_weekly_org_schedule?: WeeklyOrgSchedule;
  /**
   * The schedule must be valid and well-defined for at least this many
   * days in the future, otherwise a warning notification is generated.
   * Default is 0 if not specified.
   *
   */
  min_valid_days?: number;
  /**
   * Human-readable name.
   */
  name?: string;
  /**
   * The list of schedule fragments. It must be an ordered list of
   * contiguous fragments (the next starting when the previous one
   * ends), updates in a published schedule are only allowed to add new
   * ones to the future.
   *
   */
  org_schedule_fragments?: OrgScheduleFragmentOverview[];
  /**
   * Status determines how an item can be used. In 'draft' status an item
   * can be edited but can't be used. When 'published' the item can longer
   * be edited but can be used. 'Archived' is read-only.
   *
   */
  status: "archived" | "draft" | "published";
  /**
   * Timezone in which this is defined. Only organization schedules in
   * the same timezone can be directly combined.
   *
   */
  timezone?: string;
  /**
   * Derived field indicating when a valid organization schedule will
   * become invalid. If omitted, the schedule is already invalid. A
   * schedule is valid if it has a weekly schedule for all named periods
   * for all its schedule fragments, and if it has a schedule fragment
   * for the time period in question.
   *
   */
  valid_until?: string;
  /**
   * If the organization schedule fragment specifies that the given day
   * belongs to a named period, a weekly schedule from this list with
   * the matching name will be selected.
   *
   */
  weekly_org_schedules?: WeeklyOrgSchedule[];
};
export type UserSummary = DevUserSummary | RevUserSummary | ServiceAccountSummary | SysUserSummary;
export type DevUserSummary = UserBaseSummary;
export type UserBaseSummary = AtomBaseSummary & {
  /**
   * The user's display name. The name is non-unique and mutable.
   *
   */
  display_name?: string;
  display_picture?: ArtifactSummary;
  /**
   * Email address of the user.
   */
  email?: string;
  /**
   * Full name of the user.
   */
  full_name?: string;
  /**
   * State of the user.
   */
  state?: "active" | "deactivated" | "deleted" | "locked" | "shadow" | "unassigned";
};
export type ArtifactSummary = AtomBaseSummary;
export type RevUserSummary = UserBaseSummary & {
  /**
   * External ref is a mutable unique identifier for a user within the
   * Rev organization from your primary customer record. If none is
   * available, a good alternative is the email address/phone number
   * which could uniquely identify the user. If none is specified, a
   * system-generated identifier will be assigned to the user.
   *
   */
  external_ref?: string;
  rev_org?: OrgSummary;
};
export type OrgSummary = AccountSummary | RevOrgSummary;
export type AccountSummary = OrgBaseSummary;
export type OrgBaseSummary = AtomBaseSummary & {
  /**
   * Name of the Organization.
   */
  display_name?: string;
};
export type RevOrgSummary = OrgBaseSummary;
export type ServiceAccountSummary = UserBaseSummary;
export type SysUserSummary = UserBaseSummary;

export interface OrgSchedulesListResponse {
  /**
   * The cursor used to iterate subsequent results in accordance to the
   * sort order. If not set, then no later elements exist.
   *
   */
  next_cursor?: string;
  /**
   * The list of organization schedules.
   */
  org_schedules: OrgSchedule[];
  /**
   * The cursor used to iterate preceding results in accordance to the
   * sort order. If not set, then no prior elements exist.
   *
   */
  prev_cursor?: string;
}
export interface AtomBase {
  created_by?: UserSummary;
  /**
   * Timestamp when the object was created.
   */
  created_date?: string;
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
  modified_by?: UserSummary;
  /**
   * Timestamp when the object was last modified.
   */
  modified_date?: string;
}
export interface AtomBaseSummary {
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
}
/**
 * The schedule for each week.
 */
export interface WeeklyOrgSchedule {}
/**
 * The representation of the organization schedule fragment embedded
 * inside a organization schedule, used to quickly look up the schedule
 * fragment for the right period.
 *
 */
export interface OrgScheduleFragmentOverview {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"400"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "artifact_already_attached_to_a_parent",
  "existing_parent": "string",
  "is_same": true
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorBadRequest = ErrorBase &
  (
    | ErrorBadRequestArtifactAlreadyAttachedToAParent
    | ErrorBadRequestBadRequest
    | ErrorBadRequestInvalidApiVersion
    | ErrorBadRequestInvalidEnumValue
    | ErrorBadRequestInvalidField
    | ErrorBadRequestMissingDependency
    | ErrorBadRequestMissingRequiredField
    | ErrorBadRequestParseError
    | ErrorBadRequestStaleSchemaFragments
    | ErrorBadRequestUnexpectedJsonType
    | ErrorBadRequestValueNotPermitted
  );

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorBadRequestArtifactAlreadyAttachedToAParent {
  /**
   * The existing parent attached to the artifact.
   */
  existing_parent: string;
  /**
   * Whether the existing parent is the same as the new parent.
   *
   */
  is_same: boolean;
}
export interface ErrorBadRequestBadRequest {}
export interface ErrorBadRequestInvalidApiVersion {
  /**
   * The provided API version.
   */
  value: string;
}
export interface ErrorBadRequestInvalidEnumValue {
  /**
   * The allowed values for the field.
   */
  allowed_values: string[];
  /**
   * The field whose enum value is invalid.
   */
  field_name: string;
  /**
   * The value that was received.
   */
  value: string;
}
export interface ErrorBadRequestInvalidField {
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestMissingDependency {
  /**
   * The dependent fields.
   */
  dependencies?: ErrorBadRequestMissingDependencyDependency[];
  /**
   * The field on which the value depends.
   */
  dependent_field_name?: string;
  /**
   * The value which needs to be set of the dependent field.
   */
  dependent_field_value?: string;
  /**
   * The field whose value was received.
   */
  provided_field_name?: string;
  /**
   * The value that was received.
   */
  provided_field_value?: string;
}
export interface ErrorBadRequestMissingDependencyDependency {
  /**
   * The dependent field name.
   */
  field_name: string;
  /**
   * The dependent field value.
   */
  field_value: string;
}
export interface ErrorBadRequestMissingRequiredField {
  /**
   * The missing field's name.
   */
  field_name: string;
}
export interface ErrorBadRequestParseError {}
/**
 * Error indicating that the request contained one or more stale schema
 * fragments, which are schema fragments that have been replaced by a
 * newer version and are therefore considered deprecated schema fragments.
 * The caller should refresh and use the latest schema fragments in their
 * request.
 *
 */
export interface ErrorBadRequestStaleSchemaFragments {}
export interface ErrorBadRequestUnexpectedJsonType {
  actual: "array" | "bool" | "null" | "number" | "object" | "string";
  expected: "array" | "bool" | "null" | "number" | "object" | "string";
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestValueNotPermitted {
  /**
   * The allowed values for the field.
   */
  allowed_values?: string[];
  /**
   * The field whose value is not permitted.
   */
  field_name: string;
  /**
   * The reason the value isn't permitted.
   */
  reason?: string;
}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"401"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "unauthenticated"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorUnauthorized = ErrorBase & ErrorUnauthorizedUnauthenticated;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorUnauthorizedUnauthenticated {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"403"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "forbidden"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorForbidden = ErrorBase & ErrorForbiddenForbidden;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorForbiddenForbidden {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"429"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "retry_after": 0,
  "type": "too_many_requests"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorTooManyRequests = ErrorBase & ErrorTooManyRequestsTooManyRequests;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorTooManyRequestsTooManyRequests {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"500"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "reference_id": "string",
  "type": "internal_error"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorInternalServerError = ErrorBase & ErrorInternalServerErrorInternalError;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorInternalServerErrorInternalError {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"503"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "service_unavailable"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorServiceUnavailable = ErrorBase & ErrorServiceUnavailableServiceUnavailable;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorServiceUnavailableServiceUnavailable {}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/org-schedules.list"}>

## org-schedules-list-post

Gets list of organization schedules.

### Request Body (Optional)



<Property required={false} deprecated={false} name={"created_by_id"} type={"array of string"}>

Creator ID the filter matches.

</Property>

<Property required={false} deprecated={false} name={"cursor"} type={"string"}>

The cursor to resume iteration from. If not provided, then
iteration starts from the beginning.


</Property>

<Property required={false} deprecated={false} name={"limit"} type={"integer"}>

Max number of organization schedules returned in a page. Default is
50.


<span>Format:  `"int32"`</span>

</Property>

<Property required={false} deprecated={false} name={"mode"} type={"string"}>

The iteration mode to use. If "after", then entries after the provided
cursor will be returned, or if no cursor is provided, then from the
beginning. If "before", then entries before the provided cursor will be
returned, or if no cursor is provided, then from the end. Entries will
always be returned in the specified sort-by order.


<span>Value in:  `"after" | "before"`</span>

</Property>

<Property required={false} deprecated={false} name={"status"} type={"array of string"}>

The organization schedule statuses the filter matches.

</Property>

<Property required={false} deprecated={false} name={"valid_until"} type={"object | object | object"}>

Provides ways to specify date ranges on objects.

<Accordions>

<Accordion title={"Object Type"}>

<Property required={true} deprecated={false} name={"type"} type={"string"}>

Type of date filter.

<span>Value in:  `"preset" | "range"`</span>

</Property>

</Accordion>

</Accordions>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Success. |
| `400` | 400 Bad Request. |
| `401` | 401 Unauthorized. |
| `403` | 403 Forbidden. |
| `429` | 429 Too Many Requests. |
| `500` | 500 Internal Server Error. |
| `503` | 503 Service Unavailable. |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X POST "https://api.devrev.ai/org-schedules.list" \
  -d '{
  "created_by_id": [
    "string"
  ],
  "cursor": "string",
  "limit": 0,
  "mode": "after",
  "status": [
    "archived"
  ],
  "valid_until": {
    "type": "preset",
    "after": "2023-01-01T12:00:00.000Z",
    "before": "2023-01-01T12:00:00.000Z"
  }
}'
```

<Tabs items={["200","400","401","403","429","500","503"]}>

<Tab value={"200"}>



```json title="Example Response"
{
  "next_cursor": "string",
  "org_schedules": [
    {
      "created_by": {
        "type": "dev_user",
        "display_id": "string",
        "id": "string",
        "display_name": "string",
        "display_picture": {
          "display_id": "string",
          "id": "string"
        },
        "email": "string",
        "full_name": "string",
        "state": "active"
      },
      "created_date": "2023-01-01T12:00:00.000Z",
      "display_id": "string",
      "id": "string",
      "modified_by": {
        "type": "dev_user",
        "display_id": "string",
        "id": "string",
        "display_name": "string",
        "display_picture": {
          "display_id": "string",
          "id": "string"
        },
        "email": "string",
        "full_name": "string",
        "state": "active"
      },
      "modified_date": "2023-01-01T12:00:00.000Z",
      "default_weekly_org_schedule": {},
      "min_valid_days": 0,
      "name": "string",
      "org_schedule_fragments": [
        {}
      ],
      "status": "archived",
      "timezone": "string",
      "valid_until": "2023-01-01T12:00:00.000Z",
      "weekly_org_schedules": [
        {}
      ]
    }
  ],
  "prev_cursor": "string"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type OrgSchedule = AtomBase & {
  default_weekly_org_schedule?: WeeklyOrgSchedule;
  /**
   * The schedule must be valid and well-defined for at least this many
   * days in the future, otherwise a warning notification is generated.
   * Default is 0 if not specified.
   *
   */
  min_valid_days?: number;
  /**
   * Human-readable name.
   */
  name?: string;
  /**
   * The list of schedule fragments. It must be an ordered list of
   * contiguous fragments (the next starting when the previous one
   * ends), updates in a published schedule are only allowed to add new
   * ones to the future.
   *
   */
  org_schedule_fragments?: OrgScheduleFragmentOverview[];
  /**
   * Status determines how an item can be used. In 'draft' status an item
   * can be edited but can't be used. When 'published' the item can longer
   * be edited but can be used. 'Archived' is read-only.
   *
   */
  status: "archived" | "draft" | "published";
  /**
   * Timezone in which this is defined. Only organization schedules in
   * the same timezone can be directly combined.
   *
   */
  timezone?: string;
  /**
   * Derived field indicating when a valid organization schedule will
   * become invalid. If omitted, the schedule is already invalid. A
   * schedule is valid if it has a weekly schedule for all named periods
   * for all its schedule fragments, and if it has a schedule fragment
   * for the time period in question.
   *
   */
  valid_until?: string;
  /**
   * If the organization schedule fragment specifies that the given day
   * belongs to a named period, a weekly schedule from this list with
   * the matching name will be selected.
   *
   */
  weekly_org_schedules?: WeeklyOrgSchedule[];
};
export type UserSummary = DevUserSummary | RevUserSummary | ServiceAccountSummary | SysUserSummary;
export type DevUserSummary = UserBaseSummary;
export type UserBaseSummary = AtomBaseSummary & {
  /**
   * The user's display name. The name is non-unique and mutable.
   *
   */
  display_name?: string;
  display_picture?: ArtifactSummary;
  /**
   * Email address of the user.
   */
  email?: string;
  /**
   * Full name of the user.
   */
  full_name?: string;
  /**
   * State of the user.
   */
  state?: "active" | "deactivated" | "deleted" | "locked" | "shadow" | "unassigned";
};
export type ArtifactSummary = AtomBaseSummary;
export type RevUserSummary = UserBaseSummary & {
  /**
   * External ref is a mutable unique identifier for a user within the
   * Rev organization from your primary customer record. If none is
   * available, a good alternative is the email address/phone number
   * which could uniquely identify the user. If none is specified, a
   * system-generated identifier will be assigned to the user.
   *
   */
  external_ref?: string;
  rev_org?: OrgSummary;
};
export type OrgSummary = AccountSummary | RevOrgSummary;
export type AccountSummary = OrgBaseSummary;
export type OrgBaseSummary = AtomBaseSummary & {
  /**
   * Name of the Organization.
   */
  display_name?: string;
};
export type RevOrgSummary = OrgBaseSummary;
export type ServiceAccountSummary = UserBaseSummary;
export type SysUserSummary = UserBaseSummary;

export interface OrgSchedulesListResponse {
  /**
   * The cursor used to iterate subsequent results in accordance to the
   * sort order. If not set, then no later elements exist.
   *
   */
  next_cursor?: string;
  /**
   * The list of organization schedules.
   */
  org_schedules: OrgSchedule[];
  /**
   * The cursor used to iterate preceding results in accordance to the
   * sort order. If not set, then no prior elements exist.
   *
   */
  prev_cursor?: string;
}
export interface AtomBase {
  created_by?: UserSummary;
  /**
   * Timestamp when the object was created.
   */
  created_date?: string;
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
  modified_by?: UserSummary;
  /**
   * Timestamp when the object was last modified.
   */
  modified_date?: string;
}
export interface AtomBaseSummary {
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
}
/**
 * The schedule for each week.
 */
export interface WeeklyOrgSchedule {}
/**
 * The representation of the organization schedule fragment embedded
 * inside a organization schedule, used to quickly look up the schedule
 * fragment for the right period.
 *
 */
export interface OrgScheduleFragmentOverview {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"400"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "artifact_already_attached_to_a_parent",
  "existing_parent": "string",
  "is_same": true
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorBadRequest = ErrorBase &
  (
    | ErrorBadRequestArtifactAlreadyAttachedToAParent
    | ErrorBadRequestBadRequest
    | ErrorBadRequestInvalidApiVersion
    | ErrorBadRequestInvalidEnumValue
    | ErrorBadRequestInvalidField
    | ErrorBadRequestMissingDependency
    | ErrorBadRequestMissingRequiredField
    | ErrorBadRequestParseError
    | ErrorBadRequestStaleSchemaFragments
    | ErrorBadRequestUnexpectedJsonType
    | ErrorBadRequestValueNotPermitted
  );

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorBadRequestArtifactAlreadyAttachedToAParent {
  /**
   * The existing parent attached to the artifact.
   */
  existing_parent: string;
  /**
   * Whether the existing parent is the same as the new parent.
   *
   */
  is_same: boolean;
}
export interface ErrorBadRequestBadRequest {}
export interface ErrorBadRequestInvalidApiVersion {
  /**
   * The provided API version.
   */
  value: string;
}
export interface ErrorBadRequestInvalidEnumValue {
  /**
   * The allowed values for the field.
   */
  allowed_values: string[];
  /**
   * The field whose enum value is invalid.
   */
  field_name: string;
  /**
   * The value that was received.
   */
  value: string;
}
export interface ErrorBadRequestInvalidField {
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestMissingDependency {
  /**
   * The dependent fields.
   */
  dependencies?: ErrorBadRequestMissingDependencyDependency[];
  /**
   * The field on which the value depends.
   */
  dependent_field_name?: string;
  /**
   * The value which needs to be set of the dependent field.
   */
  dependent_field_value?: string;
  /**
   * The field whose value was received.
   */
  provided_field_name?: string;
  /**
   * The value that was received.
   */
  provided_field_value?: string;
}
export interface ErrorBadRequestMissingDependencyDependency {
  /**
   * The dependent field name.
   */
  field_name: string;
  /**
   * The dependent field value.
   */
  field_value: string;
}
export interface ErrorBadRequestMissingRequiredField {
  /**
   * The missing field's name.
   */
  field_name: string;
}
export interface ErrorBadRequestParseError {}
/**
 * Error indicating that the request contained one or more stale schema
 * fragments, which are schema fragments that have been replaced by a
 * newer version and are therefore considered deprecated schema fragments.
 * The caller should refresh and use the latest schema fragments in their
 * request.
 *
 */
export interface ErrorBadRequestStaleSchemaFragments {}
export interface ErrorBadRequestUnexpectedJsonType {
  actual: "array" | "bool" | "null" | "number" | "object" | "string";
  expected: "array" | "bool" | "null" | "number" | "object" | "string";
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestValueNotPermitted {
  /**
   * The allowed values for the field.
   */
  allowed_values?: string[];
  /**
   * The field whose value is not permitted.
   */
  field_name: string;
  /**
   * The reason the value isn't permitted.
   */
  reason?: string;
}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"401"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "unauthenticated"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorUnauthorized = ErrorBase & ErrorUnauthorizedUnauthenticated;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorUnauthorizedUnauthenticated {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"403"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "forbidden"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorForbidden = ErrorBase & ErrorForbiddenForbidden;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorForbiddenForbidden {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"429"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "retry_after": 0,
  "type": "too_many_requests"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorTooManyRequests = ErrorBase & ErrorTooManyRequestsTooManyRequests;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorTooManyRequestsTooManyRequests {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"500"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "reference_id": "string",
  "type": "internal_error"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorInternalServerError = ErrorBase & ErrorInternalServerErrorInternalError;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorInternalServerErrorInternalError {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"503"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "service_unavailable"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorServiceUnavailable = ErrorBase & ErrorServiceUnavailableServiceUnavailable;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorServiceUnavailableServiceUnavailable {}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/org-schedules.set-future"}>

## org-schedules-set-future

Sets next organization schedule fragment which must begin the day the
last existing fragment ends.


### Request Body (Optional)



<Property required={true} deprecated={false} name={"id"} type={"string"}>

Organization schedule ID.

</Property>

<Property required={true} deprecated={false} name={"org_schedule_fragment_id"} type={"string"}>

Organization schedule Fragment ID.

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Success. |
| `400` | 400 Bad Request. |
| `401` | 401 Unauthorized. |
| `403` | 403 Forbidden. |
| `429` | 429 Too Many Requests. |
| `500` | 500 Internal Server Error. |
| `503` | 503 Service Unavailable. |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X POST "https://api.devrev.ai/org-schedules.set-future" \
  -d '{
  "id": "string",
  "org_schedule_fragment_id": "string"
}'
```

<Tabs items={["200","400","401","403","429","500","503"]}>

<Tab value={"200"}>



```json title="Example Response"
{
  "org_schedule": {
    "created_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "created_date": "2023-01-01T12:00:00.000Z",
    "display_id": "string",
    "id": "string",
    "modified_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "modified_date": "2023-01-01T12:00:00.000Z",
    "default_weekly_org_schedule": {},
    "min_valid_days": 0,
    "name": "string",
    "org_schedule_fragments": [
      {}
    ],
    "status": "archived",
    "timezone": "string",
    "valid_until": "2023-01-01T12:00:00.000Z",
    "weekly_org_schedules": [
      {}
    ]
  }
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type OrgSchedule = AtomBase & {
  default_weekly_org_schedule?: WeeklyOrgSchedule;
  /**
   * The schedule must be valid and well-defined for at least this many
   * days in the future, otherwise a warning notification is generated.
   * Default is 0 if not specified.
   *
   */
  min_valid_days?: number;
  /**
   * Human-readable name.
   */
  name?: string;
  /**
   * The list of schedule fragments. It must be an ordered list of
   * contiguous fragments (the next starting when the previous one
   * ends), updates in a published schedule are only allowed to add new
   * ones to the future.
   *
   */
  org_schedule_fragments?: OrgScheduleFragmentOverview[];
  /**
   * Status determines how an item can be used. In 'draft' status an item
   * can be edited but can't be used. When 'published' the item can longer
   * be edited but can be used. 'Archived' is read-only.
   *
   */
  status: "archived" | "draft" | "published";
  /**
   * Timezone in which this is defined. Only organization schedules in
   * the same timezone can be directly combined.
   *
   */
  timezone?: string;
  /**
   * Derived field indicating when a valid organization schedule will
   * become invalid. If omitted, the schedule is already invalid. A
   * schedule is valid if it has a weekly schedule for all named periods
   * for all its schedule fragments, and if it has a schedule fragment
   * for the time period in question.
   *
   */
  valid_until?: string;
  /**
   * If the organization schedule fragment specifies that the given day
   * belongs to a named period, a weekly schedule from this list with
   * the matching name will be selected.
   *
   */
  weekly_org_schedules?: WeeklyOrgSchedule[];
};
export type UserSummary = DevUserSummary | RevUserSummary | ServiceAccountSummary | SysUserSummary;
export type DevUserSummary = UserBaseSummary;
export type UserBaseSummary = AtomBaseSummary & {
  /**
   * The user's display name. The name is non-unique and mutable.
   *
   */
  display_name?: string;
  display_picture?: ArtifactSummary;
  /**
   * Email address of the user.
   */
  email?: string;
  /**
   * Full name of the user.
   */
  full_name?: string;
  /**
   * State of the user.
   */
  state?: "active" | "deactivated" | "deleted" | "locked" | "shadow" | "unassigned";
};
export type ArtifactSummary = AtomBaseSummary;
export type RevUserSummary = UserBaseSummary & {
  /**
   * External ref is a mutable unique identifier for a user within the
   * Rev organization from your primary customer record. If none is
   * available, a good alternative is the email address/phone number
   * which could uniquely identify the user. If none is specified, a
   * system-generated identifier will be assigned to the user.
   *
   */
  external_ref?: string;
  rev_org?: OrgSummary;
};
export type OrgSummary = AccountSummary | RevOrgSummary;
export type AccountSummary = OrgBaseSummary;
export type OrgBaseSummary = AtomBaseSummary & {
  /**
   * Name of the Organization.
   */
  display_name?: string;
};
export type RevOrgSummary = OrgBaseSummary;
export type ServiceAccountSummary = UserBaseSummary;
export type SysUserSummary = UserBaseSummary;

export interface OrgSchedulesSetFutureResponse {
  org_schedule: OrgSchedule;
}
export interface AtomBase {
  created_by?: UserSummary;
  /**
   * Timestamp when the object was created.
   */
  created_date?: string;
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
  modified_by?: UserSummary;
  /**
   * Timestamp when the object was last modified.
   */
  modified_date?: string;
}
export interface AtomBaseSummary {
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
}
/**
 * The schedule for each week.
 */
export interface WeeklyOrgSchedule {}
/**
 * The representation of the organization schedule fragment embedded
 * inside a organization schedule, used to quickly look up the schedule
 * fragment for the right period.
 *
 */
export interface OrgScheduleFragmentOverview {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"400"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "artifact_already_attached_to_a_parent",
  "existing_parent": "string",
  "is_same": true
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorBadRequest = ErrorBase &
  (
    | ErrorBadRequestArtifactAlreadyAttachedToAParent
    | ErrorBadRequestBadRequest
    | ErrorBadRequestInvalidApiVersion
    | ErrorBadRequestInvalidEnumValue
    | ErrorBadRequestInvalidField
    | ErrorBadRequestMissingDependency
    | ErrorBadRequestMissingRequiredField
    | ErrorBadRequestParseError
    | ErrorBadRequestStaleSchemaFragments
    | ErrorBadRequestUnexpectedJsonType
    | ErrorBadRequestValueNotPermitted
  );

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorBadRequestArtifactAlreadyAttachedToAParent {
  /**
   * The existing parent attached to the artifact.
   */
  existing_parent: string;
  /**
   * Whether the existing parent is the same as the new parent.
   *
   */
  is_same: boolean;
}
export interface ErrorBadRequestBadRequest {}
export interface ErrorBadRequestInvalidApiVersion {
  /**
   * The provided API version.
   */
  value: string;
}
export interface ErrorBadRequestInvalidEnumValue {
  /**
   * The allowed values for the field.
   */
  allowed_values: string[];
  /**
   * The field whose enum value is invalid.
   */
  field_name: string;
  /**
   * The value that was received.
   */
  value: string;
}
export interface ErrorBadRequestInvalidField {
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestMissingDependency {
  /**
   * The dependent fields.
   */
  dependencies?: ErrorBadRequestMissingDependencyDependency[];
  /**
   * The field on which the value depends.
   */
  dependent_field_name?: string;
  /**
   * The value which needs to be set of the dependent field.
   */
  dependent_field_value?: string;
  /**
   * The field whose value was received.
   */
  provided_field_name?: string;
  /**
   * The value that was received.
   */
  provided_field_value?: string;
}
export interface ErrorBadRequestMissingDependencyDependency {
  /**
   * The dependent field name.
   */
  field_name: string;
  /**
   * The dependent field value.
   */
  field_value: string;
}
export interface ErrorBadRequestMissingRequiredField {
  /**
   * The missing field's name.
   */
  field_name: string;
}
export interface ErrorBadRequestParseError {}
/**
 * Error indicating that the request contained one or more stale schema
 * fragments, which are schema fragments that have been replaced by a
 * newer version and are therefore considered deprecated schema fragments.
 * The caller should refresh and use the latest schema fragments in their
 * request.
 *
 */
export interface ErrorBadRequestStaleSchemaFragments {}
export interface ErrorBadRequestUnexpectedJsonType {
  actual: "array" | "bool" | "null" | "number" | "object" | "string";
  expected: "array" | "bool" | "null" | "number" | "object" | "string";
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestValueNotPermitted {
  /**
   * The allowed values for the field.
   */
  allowed_values?: string[];
  /**
   * The field whose value is not permitted.
   */
  field_name: string;
  /**
   * The reason the value isn't permitted.
   */
  reason?: string;
}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"401"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "unauthenticated"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorUnauthorized = ErrorBase & ErrorUnauthorizedUnauthenticated;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorUnauthorizedUnauthenticated {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"403"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "forbidden"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorForbidden = ErrorBase & ErrorForbiddenForbidden;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorForbiddenForbidden {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"429"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "retry_after": 0,
  "type": "too_many_requests"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorTooManyRequests = ErrorBase & ErrorTooManyRequestsTooManyRequests;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorTooManyRequestsTooManyRequests {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"500"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "reference_id": "string",
  "type": "internal_error"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorInternalServerError = ErrorBase & ErrorInternalServerErrorInternalError;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorInternalServerErrorInternalError {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"503"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "service_unavailable"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorServiceUnavailable = ErrorBase & ErrorServiceUnavailableServiceUnavailable;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorServiceUnavailableServiceUnavailable {}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/org-schedules.transition"}>

## org-schedules-transition

Publishes or archives an organization schedule.

### Request Body (Optional)



<Property required={true} deprecated={false} name={"id"} type={"string"}>

Organization schedule ID.

</Property>

<Property required={true} deprecated={false} name={"status"} type={"string"}>

Status determines how an item can be used. In 'draft' status an item
can be edited but can't be used. When 'published' the item can longer
be edited but can be used. 'Archived' is read-only.


<span>Value in:  `"archived" | "draft" | "published"`</span>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `200` | Success. |
| `400` | 400 Bad Request. |
| `401` | 401 Unauthorized. |
| `403` | 403 Forbidden. |
| `429` | 429 Too Many Requests. |
| `500` | 500 Internal Server Error. |
| `503` | 503 Service Unavailable. |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X POST "https://api.devrev.ai/org-schedules.transition" \
  -d '{
  "id": "string",
  "status": "archived"
}'
```

<Tabs items={["200","400","401","403","429","500","503"]}>

<Tab value={"200"}>



```json title="Example Response"
{
  "org_schedule": {
    "created_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "created_date": "2023-01-01T12:00:00.000Z",
    "display_id": "string",
    "id": "string",
    "modified_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "modified_date": "2023-01-01T12:00:00.000Z",
    "default_weekly_org_schedule": {},
    "min_valid_days": 0,
    "name": "string",
    "org_schedule_fragments": [
      {}
    ],
    "status": "archived",
    "timezone": "string",
    "valid_until": "2023-01-01T12:00:00.000Z",
    "weekly_org_schedules": [
      {}
    ]
  }
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type OrgSchedule = AtomBase & {
  default_weekly_org_schedule?: WeeklyOrgSchedule;
  /**
   * The schedule must be valid and well-defined for at least this many
   * days in the future, otherwise a warning notification is generated.
   * Default is 0 if not specified.
   *
   */
  min_valid_days?: number;
  /**
   * Human-readable name.
   */
  name?: string;
  /**
   * The list of schedule fragments. It must be an ordered list of
   * contiguous fragments (the next starting when the previous one
   * ends), updates in a published schedule are only allowed to add new
   * ones to the future.
   *
   */
  org_schedule_fragments?: OrgScheduleFragmentOverview[];
  /**
   * Status determines how an item can be used. In 'draft' status an item
   * can be edited but can't be used. When 'published' the item can longer
   * be edited but can be used. 'Archived' is read-only.
   *
   */
  status: "archived" | "draft" | "published";
  /**
   * Timezone in which this is defined. Only organization schedules in
   * the same timezone can be directly combined.
   *
   */
  timezone?: string;
  /**
   * Derived field indicating when a valid organization schedule will
   * become invalid. If omitted, the schedule is already invalid. A
   * schedule is valid if it has a weekly schedule for all named periods
   * for all its schedule fragments, and if it has a schedule fragment
   * for the time period in question.
   *
   */
  valid_until?: string;
  /**
   * If the organization schedule fragment specifies that the given day
   * belongs to a named period, a weekly schedule from this list with
   * the matching name will be selected.
   *
   */
  weekly_org_schedules?: WeeklyOrgSchedule[];
};
export type UserSummary = DevUserSummary | RevUserSummary | ServiceAccountSummary | SysUserSummary;
export type DevUserSummary = UserBaseSummary;
export type UserBaseSummary = AtomBaseSummary & {
  /**
   * The user's display name. The name is non-unique and mutable.
   *
   */
  display_name?: string;
  display_picture?: ArtifactSummary;
  /**
   * Email address of the user.
   */
  email?: string;
  /**
   * Full name of the user.
   */
  full_name?: string;
  /**
   * State of the user.
   */
  state?: "active" | "deactivated" | "deleted" | "locked" | "shadow" | "unassigned";
};
export type ArtifactSummary = AtomBaseSummary;
export type RevUserSummary = UserBaseSummary & {
  /**
   * External ref is a mutable unique identifier for a user within the
   * Rev organization from your primary customer record. If none is
   * available, a good alternative is the email address/phone number
   * which could uniquely identify the user. If none is specified, a
   * system-generated identifier will be assigned to the user.
   *
   */
  external_ref?: string;
  rev_org?: OrgSummary;
};
export type OrgSummary = AccountSummary | RevOrgSummary;
export type AccountSummary = OrgBaseSummary;
export type OrgBaseSummary = AtomBaseSummary & {
  /**
   * Name of the Organization.
   */
  display_name?: string;
};
export type RevOrgSummary = OrgBaseSummary;
export type ServiceAccountSummary = UserBaseSummary;
export type SysUserSummary = UserBaseSummary;

export interface OrgSchedulesTransitionResponse {
  org_schedule: OrgSchedule;
}
export interface AtomBase {
  created_by?: UserSummary;
  /**
   * Timestamp when the object was created.
   */
  created_date?: string;
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
  modified_by?: UserSummary;
  /**
   * Timestamp when the object was last modified.
   */
  modified_date?: string;
}
export interface AtomBaseSummary {
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
}
/**
 * The schedule for each week.
 */
export interface WeeklyOrgSchedule {}
/**
 * The representation of the organization schedule fragment embedded
 * inside a organization schedule, used to quickly look up the schedule
 * fragment for the right period.
 *
 */
export interface OrgScheduleFragmentOverview {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"400"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "artifact_already_attached_to_a_parent",
  "existing_parent": "string",
  "is_same": true
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorBadRequest = ErrorBase &
  (
    | ErrorBadRequestArtifactAlreadyAttachedToAParent
    | ErrorBadRequestBadRequest
    | ErrorBadRequestInvalidApiVersion
    | ErrorBadRequestInvalidEnumValue
    | ErrorBadRequestInvalidField
    | ErrorBadRequestMissingDependency
    | ErrorBadRequestMissingRequiredField
    | ErrorBadRequestParseError
    | ErrorBadRequestStaleSchemaFragments
    | ErrorBadRequestUnexpectedJsonType
    | ErrorBadRequestValueNotPermitted
  );

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorBadRequestArtifactAlreadyAttachedToAParent {
  /**
   * The existing parent attached to the artifact.
   */
  existing_parent: string;
  /**
   * Whether the existing parent is the same as the new parent.
   *
   */
  is_same: boolean;
}
export interface ErrorBadRequestBadRequest {}
export interface ErrorBadRequestInvalidApiVersion {
  /**
   * The provided API version.
   */
  value: string;
}
export interface ErrorBadRequestInvalidEnumValue {
  /**
   * The allowed values for the field.
   */
  allowed_values: string[];
  /**
   * The field whose enum value is invalid.
   */
  field_name: string;
  /**
   * The value that was received.
   */
  value: string;
}
export interface ErrorBadRequestInvalidField {
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestMissingDependency {
  /**
   * The dependent fields.
   */
  dependencies?: ErrorBadRequestMissingDependencyDependency[];
  /**
   * The field on which the value depends.
   */
  dependent_field_name?: string;
  /**
   * The value which needs to be set of the dependent field.
   */
  dependent_field_value?: string;
  /**
   * The field whose value was received.
   */
  provided_field_name?: string;
  /**
   * The value that was received.
   */
  provided_field_value?: string;
}
export interface ErrorBadRequestMissingDependencyDependency {
  /**
   * The dependent field name.
   */
  field_name: string;
  /**
   * The dependent field value.
   */
  field_value: string;
}
export interface ErrorBadRequestMissingRequiredField {
  /**
   * The missing field's name.
   */
  field_name: string;
}
export interface ErrorBadRequestParseError {}
/**
 * Error indicating that the request contained one or more stale schema
 * fragments, which are schema fragments that have been replaced by a
 * newer version and are therefore considered deprecated schema fragments.
 * The caller should refresh and use the latest schema fragments in their
 * request.
 *
 */
export interface ErrorBadRequestStaleSchemaFragments {}
export interface ErrorBadRequestUnexpectedJsonType {
  actual: "array" | "bool" | "null" | "number" | "object" | "string";
  expected: "array" | "bool" | "null" | "number" | "object" | "string";
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestValueNotPermitted {
  /**
   * The allowed values for the field.
   */
  allowed_values?: string[];
  /**
   * The field whose value is not permitted.
   */
  field_name: string;
  /**
   * The reason the value isn't permitted.
   */
  reason?: string;
}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"401"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "unauthenticated"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorUnauthorized = ErrorBase & ErrorUnauthorizedUnauthenticated;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorUnauthorizedUnauthenticated {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"403"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "forbidden"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorForbidden = ErrorBase & ErrorForbiddenForbidden;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorForbiddenForbidden {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"429"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "retry_after": 0,
  "type": "too_many_requests"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorTooManyRequests = ErrorBase & ErrorTooManyRequestsTooManyRequests;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorTooManyRequestsTooManyRequests {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"500"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "reference_id": "string",
  "type": "internal_error"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorInternalServerError = ErrorBase & ErrorInternalServerErrorInternalError;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorInternalServerErrorInternalError {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"503"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "service_unavailable"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorServiceUnavailable = ErrorBase & ErrorServiceUnavailableServiceUnavailable;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorServiceUnavailableServiceUnavailable {}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

<API>

<APIInfo method={"POST"} route={"/org-schedules.update"}>

## org-schedules-update

Updates an organization schedule.

### Request Body (Optional)



<Property required={false} deprecated={false} name={"default_weekly_org_schedule"} type={"object"}>



<Accordions>

<Accordion title={"Object Type"}>

<Property required={true} deprecated={false} name={"intervals"} type={"array of object"}>

The 'on' intervals of the week.

<Accordions>

<Accordion title={"Object Type"}>

<Property required={true} deprecated={false} name={"from"} type={"integer"}>

Duration in minutes of the week when the interval starts. 0 is
Sunday midnight, when Sunday ends and Monday begins.


<span>Format:  `"int64"`</span>

</Property>

<Property required={true} deprecated={false} name={"to"} type={"integer"}>

Duration in minutes of the week when the interval ends (must be
larger than 'from'). 0 is Sunday midnight, when Sunday ends and
Monday begins.


<span>Format:  `"int64"`</span>

</Property>

</Accordion>

</Accordions>

</Property>

<Property required={true} deprecated={false} name={"period_name"} type={"string"}>

The name of the period during which the organization schedule
applies.


</Property>

</Accordion>

</Accordions>

</Property>

<Property required={true} deprecated={false} name={"id"} type={"string"}>

Organization schedule ID.

</Property>

<Property required={false} deprecated={false} name={"min_valid_days"} type={"integer"}>

Organization schedule must be valid for at least this many days in
the future. Meaning organization schedule fragments must cover this
period. 0 if omitted.


<span>Format:  `"int64"`</span>

</Property>

<Property required={false} deprecated={false} name={"name"} type={"string"}>

Human-readable name.

</Property>

<Property required={false} deprecated={false} name={"org_schedule_fragments"} type={"array of object"}>

List of organization schedule fragments with no overlaps or gaps.


<Accordions>

<Accordion title={"Object Type"}>

<Property required={true} deprecated={false} name={"id"} type={"string"}>

Organization schedule fragment ID.

</Property>

</Accordion>

</Accordions>

</Property>

<Property required={false} deprecated={false} name={"timezone"} type={"string"}>

Timezone in which the organization schedule applies. Expected to be
a valid IANA time zone name such as America/New_York.


</Property>

<Property required={false} deprecated={false} name={"weekly_org_schedules"} type={"array of object"}>

If this day belongs to a named period according to the currently
active organization schedule fragment, a weekly organization
schedule from this list with the corresponding name will apply.


<Accordions>

<Accordion title={"Object Type"}>

<Property required={true} deprecated={false} name={"intervals"} type={"array of object"}>

The 'on' intervals of the week.

<Accordions>

<Accordion title={"Object Type"}>

<Property required={true} deprecated={false} name={"from"} type={"integer"}>

Duration in minutes of the week when the interval starts. 0 is
Sunday midnight, when Sunday ends and Monday begins.


<span>Format:  `"int64"`</span>

</Property>

<Property required={true} deprecated={false} name={"to"} type={"integer"}>

Duration in minutes of the week when the interval ends (must be
larger than 'from'). 0 is Sunday midnight, when Sunday ends and
Monday begins.


<span>Format:  `"int64"`</span>

</Property>

</Accordion>

</Accordions>

</Property>

<Property required={true} deprecated={false} name={"period_name"} type={"string"}>

The name of the period during which the organization schedule
applies.


</Property>

</Accordion>

</Accordions>

</Property>

| Status code | Description |
| ----------- | ----------- |
| `201` | Success. |
| `400` | 400 Bad Request. |
| `401` | 401 Unauthorized. |
| `403` | 403 Forbidden. |
| `404` | 404 Not Found. |
| `429` | 429 Too Many Requests. |
| `500` | 500 Internal Server Error. |
| `503` | 503 Service Unavailable. |

</APIInfo>

<APIExample>

```bash title="curl"
curl -X POST "https://api.devrev.ai/org-schedules.update" \
  -d '{
  "default_weekly_org_schedule": {
    "intervals": [
      {
        "from": 0,
        "to": 0
      }
    ],
    "period_name": "string"
  },
  "id": "string",
  "min_valid_days": 0,
  "name": "string",
  "org_schedule_fragments": [
    {
      "id": "string"
    }
  ],
  "timezone": "string",
  "weekly_org_schedules": [
    {
      "intervals": [
        {
          "from": 0,
          "to": 0
        }
      ],
      "period_name": "string"
    }
  ]
}'
```

<Tabs items={["201","400","401","403","404","429","500","503"]}>

<Tab value={"201"}>



```json title="Example Response"
{
  "org_schedule": {
    "created_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "created_date": "2023-01-01T12:00:00.000Z",
    "display_id": "string",
    "id": "string",
    "modified_by": {
      "type": "dev_user",
      "display_id": "string",
      "id": "string",
      "display_name": "string",
      "display_picture": {
        "display_id": "string",
        "id": "string"
      },
      "email": "string",
      "full_name": "string",
      "state": "active"
    },
    "modified_date": "2023-01-01T12:00:00.000Z",
    "default_weekly_org_schedule": {},
    "min_valid_days": 0,
    "name": "string",
    "org_schedule_fragments": [
      {}
    ],
    "status": "archived",
    "timezone": "string",
    "valid_until": "2023-01-01T12:00:00.000Z",
    "weekly_org_schedules": [
      {}
    ]
  }
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type OrgSchedule = AtomBase & {
  default_weekly_org_schedule?: WeeklyOrgSchedule;
  /**
   * The schedule must be valid and well-defined for at least this many
   * days in the future, otherwise a warning notification is generated.
   * Default is 0 if not specified.
   *
   */
  min_valid_days?: number;
  /**
   * Human-readable name.
   */
  name?: string;
  /**
   * The list of schedule fragments. It must be an ordered list of
   * contiguous fragments (the next starting when the previous one
   * ends), updates in a published schedule are only allowed to add new
   * ones to the future.
   *
   */
  org_schedule_fragments?: OrgScheduleFragmentOverview[];
  /**
   * Status determines how an item can be used. In 'draft' status an item
   * can be edited but can't be used. When 'published' the item can longer
   * be edited but can be used. 'Archived' is read-only.
   *
   */
  status: "archived" | "draft" | "published";
  /**
   * Timezone in which this is defined. Only organization schedules in
   * the same timezone can be directly combined.
   *
   */
  timezone?: string;
  /**
   * Derived field indicating when a valid organization schedule will
   * become invalid. If omitted, the schedule is already invalid. A
   * schedule is valid if it has a weekly schedule for all named periods
   * for all its schedule fragments, and if it has a schedule fragment
   * for the time period in question.
   *
   */
  valid_until?: string;
  /**
   * If the organization schedule fragment specifies that the given day
   * belongs to a named period, a weekly schedule from this list with
   * the matching name will be selected.
   *
   */
  weekly_org_schedules?: WeeklyOrgSchedule[];
};
export type UserSummary = DevUserSummary | RevUserSummary | ServiceAccountSummary | SysUserSummary;
export type DevUserSummary = UserBaseSummary;
export type UserBaseSummary = AtomBaseSummary & {
  /**
   * The user's display name. The name is non-unique and mutable.
   *
   */
  display_name?: string;
  display_picture?: ArtifactSummary;
  /**
   * Email address of the user.
   */
  email?: string;
  /**
   * Full name of the user.
   */
  full_name?: string;
  /**
   * State of the user.
   */
  state?: "active" | "deactivated" | "deleted" | "locked" | "shadow" | "unassigned";
};
export type ArtifactSummary = AtomBaseSummary;
export type RevUserSummary = UserBaseSummary & {
  /**
   * External ref is a mutable unique identifier for a user within the
   * Rev organization from your primary customer record. If none is
   * available, a good alternative is the email address/phone number
   * which could uniquely identify the user. If none is specified, a
   * system-generated identifier will be assigned to the user.
   *
   */
  external_ref?: string;
  rev_org?: OrgSummary;
};
export type OrgSummary = AccountSummary | RevOrgSummary;
export type AccountSummary = OrgBaseSummary;
export type OrgBaseSummary = AtomBaseSummary & {
  /**
   * Name of the Organization.
   */
  display_name?: string;
};
export type RevOrgSummary = OrgBaseSummary;
export type ServiceAccountSummary = UserBaseSummary;
export type SysUserSummary = UserBaseSummary;

export interface OrgSchedulesUpdateResponse {
  org_schedule: OrgSchedule;
}
export interface AtomBase {
  created_by?: UserSummary;
  /**
   * Timestamp when the object was created.
   */
  created_date?: string;
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
  modified_by?: UserSummary;
  /**
   * Timestamp when the object was last modified.
   */
  modified_date?: string;
}
export interface AtomBaseSummary {
  /**
   * Human-readable object ID unique to the Dev organization.
   */
  display_id?: string;
  /**
   * Globally unique object ID.
   */
  id: string;
}
/**
 * The schedule for each week.
 */
export interface WeeklyOrgSchedule {}
/**
 * The representation of the organization schedule fragment embedded
 * inside a organization schedule, used to quickly look up the schedule
 * fragment for the right period.
 *
 */
export interface OrgScheduleFragmentOverview {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"400"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "artifact_already_attached_to_a_parent",
  "existing_parent": "string",
  "is_same": true
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorBadRequest = ErrorBase &
  (
    | ErrorBadRequestArtifactAlreadyAttachedToAParent
    | ErrorBadRequestBadRequest
    | ErrorBadRequestInvalidApiVersion
    | ErrorBadRequestInvalidEnumValue
    | ErrorBadRequestInvalidField
    | ErrorBadRequestMissingDependency
    | ErrorBadRequestMissingRequiredField
    | ErrorBadRequestParseError
    | ErrorBadRequestStaleSchemaFragments
    | ErrorBadRequestUnexpectedJsonType
    | ErrorBadRequestValueNotPermitted
  );

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorBadRequestArtifactAlreadyAttachedToAParent {
  /**
   * The existing parent attached to the artifact.
   */
  existing_parent: string;
  /**
   * Whether the existing parent is the same as the new parent.
   *
   */
  is_same: boolean;
}
export interface ErrorBadRequestBadRequest {}
export interface ErrorBadRequestInvalidApiVersion {
  /**
   * The provided API version.
   */
  value: string;
}
export interface ErrorBadRequestInvalidEnumValue {
  /**
   * The allowed values for the field.
   */
  allowed_values: string[];
  /**
   * The field whose enum value is invalid.
   */
  field_name: string;
  /**
   * The value that was received.
   */
  value: string;
}
export interface ErrorBadRequestInvalidField {
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestMissingDependency {
  /**
   * The dependent fields.
   */
  dependencies?: ErrorBadRequestMissingDependencyDependency[];
  /**
   * The field on which the value depends.
   */
  dependent_field_name?: string;
  /**
   * The value which needs to be set of the dependent field.
   */
  dependent_field_value?: string;
  /**
   * The field whose value was received.
   */
  provided_field_name?: string;
  /**
   * The value that was received.
   */
  provided_field_value?: string;
}
export interface ErrorBadRequestMissingDependencyDependency {
  /**
   * The dependent field name.
   */
  field_name: string;
  /**
   * The dependent field value.
   */
  field_value: string;
}
export interface ErrorBadRequestMissingRequiredField {
  /**
   * The missing field's name.
   */
  field_name: string;
}
export interface ErrorBadRequestParseError {}
/**
 * Error indicating that the request contained one or more stale schema
 * fragments, which are schema fragments that have been replaced by a
 * newer version and are therefore considered deprecated schema fragments.
 * The caller should refresh and use the latest schema fragments in their
 * request.
 *
 */
export interface ErrorBadRequestStaleSchemaFragments {}
export interface ErrorBadRequestUnexpectedJsonType {
  actual: "array" | "bool" | "null" | "number" | "object" | "string";
  expected: "array" | "bool" | "null" | "number" | "object" | "string";
  /**
   * The field name that's invalid.
   */
  field_name: string;
}
export interface ErrorBadRequestValueNotPermitted {
  /**
   * The allowed values for the field.
   */
  allowed_values?: string[];
  /**
   * The field whose value is not permitted.
   */
  field_name: string;
  /**
   * The reason the value isn't permitted.
   */
  reason?: string;
}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"401"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "unauthenticated"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorUnauthorized = ErrorBase & ErrorUnauthorizedUnauthenticated;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorUnauthorizedUnauthenticated {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"403"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "forbidden"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorForbidden = ErrorBase & ErrorForbiddenForbidden;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorForbiddenForbidden {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"404"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "not_found"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorNotFound = ErrorBase & ErrorNotFoundNotFound;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorNotFoundNotFound {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"429"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "retry_after": 0,
  "type": "too_many_requests"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorTooManyRequests = ErrorBase & ErrorTooManyRequestsTooManyRequests;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorTooManyRequestsTooManyRequests {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"500"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "reference_id": "string",
  "type": "internal_error"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorInternalServerError = ErrorBase & ErrorInternalServerErrorInternalError;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorInternalServerErrorInternalError {}

```

</Accordion>

</Accordions>

</Tab>

<Tab value={"503"}>



```json title="Example Response"
{
  "detail": "string",
  "message": "string",
  "type": "service_unavailable"
}
```

<Accordions>

<Accordion title={"Typescript Definition"}>

```ts
export type ErrorServiceUnavailable = ErrorBase & ErrorServiceUnavailableServiceUnavailable;

export interface ErrorBase {
  /**
   * Error detail information.
   */
  detail?: string;
  /**
   * The message associated with the error.
   */
  message?: string;
}
export interface ErrorServiceUnavailableServiceUnavailable {}

```

</Accordion>

</Accordions>

</Tab>

</Tabs>

</APIExample>

</API>

</Root>